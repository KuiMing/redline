# Next session start prompt（P1 UI 修正 + 規則落差修正，合併派工，2026-07-11 版）

> **本檔已由 `docs/records/NEXT_SESSION_START_20260712.md` 取代**——第二批（規則落差）與 P1 後端規則項目已於 2026-07-12 全部完成，剩餘工作（P1 UI 批次、P2 驗證腳本保養、第四梯隊）請用新檔派工。

> 本檔取代 `docs/records/NEXT_SESSION_START_20260710.md`（該檔的第二批順序是第一輪盤點的舊排序；本檔已改用第二輪盤點的合併優先順序，並反映截至 2026-07-11 已完成的 7 項修正）。

把下面這段貼到新 session 即可：

```text
請到 `/Users/benmini/.openclaw/workspace/redline`。這次工作分兩批，請依序做完第一批、commit 過後，再開始第二批，不要交錯進行。

===== 全程 git 紀律（必守）=====
- 開工前先 `git status --short`。repo 裡有既有的無關 dirty/untracked 檔案（playtest-flow 驗證紀錄、purchase/event-cards 紀錄等），不屬於你的工作範圍，絕不能混進 commit。
- **禁止使用 `git add -A`、`git add .`、`git add *` 等整包全加的指令**；一律先用 `git diff HEAD -- <檔案>` 確認每個檔案的 diff 只含本次修正，再明確列出檔名 `git add <file1> <file2> ...`。
- commit 用 `git commit -m "..." -- <同一份明確檔案清單>`；注意 `git commit -- <路徑>` 會把該路徑「工作目錄當下的完整內容」重新 stage，所以 commit 前務必確認該檔工作目錄 diff 就是你要的內容。
- 每完成 1 項（或邏輯相關的幾項）就單獨 commit 一次，不要累積。
- commit 後用 `git show --stat` + `git status --short` 確認只有預期檔案入庫、無關檔案原封不動。

===== 第一批：P1 Playtest UI 三項修正 =====

先讀 `docs/records/playtest-feedback/PLAN_P1_UI_FIXES_20260709.md`（完整背景、程式碼位置與建議做法都在裡面），照它處理：
1. 地圖移動/可達城鎮高亮不要畫大量放射狀直線（所有移動流程都要符合，不限單一卡牌）。
2. 選定組織後點可移動城鎮，先跳出確認；確認才送 move，取消要保留選擇狀態。
3. Lobby 房間代碼複製功能保留一個，移除最上方重複入口。

注意：項目1的程式修正目前以 staged-未commit 狀態掛在 working tree（`static/leaflet_game_map_logic.js` 等），照規劃書第0節先用 `git restore --staged .` 攤平再逐項處理。每項補 root cause、修正摘要、驗證指令與 browser proof（放 `docs/records/...`），完成後把 `TODO.md` 對應項目改 `[done]`，只 stage 相關檔案/hunks 後 commit。

===== 第二批：規則資料 vs 程式實作落差修正 =====

先讀兩份盤點報告（權威依據，root cause 與程式碼位置都在裡面，照報告查證後動工，不要重新猜測）：
- `docs/records/rules-audit/RULES_VS_IMPLEMENTATION_GAP_AUDIT_20260710.md`（第一輪；「進度狀態」區塊列出已完成的 7 項與 1 項誤判排除，已完成的不要重做）
- `docs/records/rules-audit/RULES_VS_IMPLEMENTATION_GAP_AUDIT_SECOND_PASS_20260711.md`（第二輪；S1-S7 新發現 + 合併優先順序，**以此為準**）

依以下合併順序逐項處理（同梯隊內可自行排序，不可跳過前面梯隊）：

第一梯隊（規則覆蓋率／勝負正確性）：
1. ~~A1：45個陣營勝利條件~~ ——**已於 2026-07-11 全部完成，跳過**（資料轉換＋victory.py 擴充＋端到端驗證 7/7）。**第二批（規則落差）至此全部完成**，僅剩第四梯隊保養項。
2. ~~S1：組織棋供應上限完全未實作~~ ——**已於 2026-07-11 修正並驗證，跳過**（反共22；紅軍依使用者決定=固定40，非規則書公式）。
3. ~~S2：新疆社會管控零實作~~ ——**已於 2026-07-11 修正並驗證，跳過**（含組織經驗甲「牆內距離1格」降級子句；勘誤：影響全部四個維吾爾變體）。組織經驗甲剩「棄4點以上可重複建立」子句在第二梯隊第7項。

第二梯隊（明確缺口）：
4. ~~A2 剩餘~~ ——**A2 已於 2026-07-11 全部完成並驗證，跳過**（紅軍派系＋歸屬檢查、民族祭儀/賭徒耳語三個文字偏差），詳見第一輪報告 A2 段落。
5. ~~S4：開局額外卡應洗入起始牌庫，而非放進棄牌堆~~ ——**已於 2026-07-11 修正並驗證，跳過**；「宣傳卡vs宣傳家」用語（S4 附帶，第三梯隊）仍待確認。
6. ~~S3：事件牌庫應混洗後抽出20張~~ ——**已於 2026-07-11 修正並驗證，跳過**（勘誤：全池是25張非33張，詳見第二輪報告 S3 段落）；C2（歲月靜好至多抽除5張的難度選項）仍待處理，歸入第四梯隊。
7. ~~B3 剩餘~~ ——**全部完成（2026-07-11），跳過**（組織經驗甲兩段子句＋確認流程、行動預告/行動募資頂牌選擇權；組織經驗甲的 P1 playtest 確認流程項目也一併完成）。

第三梯隊（**先確認語意再動工**——這些項目請把疑問整理成清單回報，等使用者確認後才實作，不要自行假設）：
8. ~~B1-c：II 級 AND/OR 語意~~ ——**已裁決為 OR 並於 2026-07-11 修正，跳過**。
9. ~~S5~~ ——**依 2026-08-14 最新裁決完成，跳過**（赤鱲角機場只移動組織；事件卡「香港抗暴之戰」結算後才可免費前移根據地；不存在「香港抗爭之烈」或機場付費遷移根據地）。前端根據地按鈕與免費窗口提示已完成。
10. ~~C1：內鬥可改雙倍分神~~ ——**已裁決（供應替代）並於 2026-07-11 實作，跳過**。
11. ~~A4：共同勝利語意~~ ——**已裁決並於 2026-07-11 實作，跳過**（反共玩家獲勝時其他反共玩家進度 >= 2/3 為共同勝利者；UI 顯示歸 P1「第20回合勝利宣告」項目）。
12. ~~S4 附帶：「宣傳卡」所指~~ ——**已裁決＝宣傳家（現行實作正確），關閉**。

第四梯隊（保養性質，最後做）：
13. C3（購買區組成資料校驗腳本）、S6（無限行動處理規則，需先回查完整規則書）、`凝聚共識`/`武裝集團` 的死代碼 JSON 宣告清理。

執行規範：
- 每項都要有 root cause、修正摘要、驗證指令與 proof（比照 `TODO.md` 現有條目風格），完成一項就更新 `TODO.md`（原本沒列的依報告描述新增），並同步更新兩份盤點報告的狀態標記（對應段落加「已修正」+ 進度狀態區塊 + 本提示詞檔劃掉該項）。
- 做不完全部沒關係；做到哪裡就把「已完成/進行中/待處理」確實記錄到 `TODO.md` 與第二輪報告末尾，方便下一個人接手。

備註：`TODO.md` 全程是權威追蹤來源；兩份盤點報告是落差清單依據；`docs/records/playtest-feedback/PLAN_P1_UI_FIXES_20260709.md` 是第一批依據；本檔只是把全部工作串起來的啟動捷徑。
```
