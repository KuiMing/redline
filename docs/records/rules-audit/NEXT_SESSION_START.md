# Next session start prompt（規則落差修正）

把下面這段貼到新 session 即可：

```text
請到 `/Users/benmini/.openclaw/workspace/redline`，先讀 `docs/records/rules-audit/RULES_VS_IMPLEMENTATION_GAP_AUDIT_20260710.md`（規則資料 vs 程式實作落差盤點報告），再讀 `TODO.md`（本專案權威追蹤清單，目前這份盤點裡的項目大多還沒被列進去）。

請依照盤點報告裡「建議的後續改善優先順序」逐項處理，梯隊順序如下（同一梯隊內可自行排序，但不要跳過前面梯隊直接做後面梯隊；每個項目的完整背景、root cause 與程式碼位置都寫在盤點報告對應章節，不要重新猜測，直接照報告內容查證後動工）：

第一梯隊（影響規則覆蓋率，優先處理）：
1. A1：46個陣營（所有只有 win_condition_text、沒有結構化 win_conditions 的地域/政治類反賊陣營）勝利條件完全未被判定。
2. ~~B1-a：`東洋奧援` 的中介資料檔已損毀/過期~~ ——**已於 2026-07-10 修正並驗證，跳過**，詳見盤點報告該段落與 `TODO.md`。

第二梯隊（明確缺口，修法清楚）：
3. A2：3個能力字串（非暴力／紅軍派系／民族祭儀）未被解析，影響15個陣營。
4. A3：粵／澳門與香港的共用組織規則只有單向生效。
5. `派遣間諜`：犧牲組織與尋找目標順序顛倒。
6. `樹立信心`：條件判定看卡牌種類而非購買費用組成（跟已追蹤的 `點燃熱情` 同根因，建議一起修）。
7. `組織經驗甲`：TODO.md 現有描述只涵蓋確認流程，實際還有兩段規則子句完全未實作，需要一併補上並更新 TODO 描述範圍。
8. B1-b：`南洋奧援` I級棄牌邏輯等於沒抽沒棄。
9. `情報網`：非反應時機下選項C選了沒效果，應停用/隱藏。

第三梯隊（UI/選擇權相關，可與既有 P1 UI 修正一起排）：
10. `行動預告`／`行動募資`：本回合買超過1張時沒有選擇要頂哪張牌。
11. `企業人脈`：借用範圍誤排除常設購買區。

第四梯隊（需要先確認規則語意，或優先度較低的保養性質）：
12. B1-c：跨7張區域門檻奧援卡的 AND/OR 語意——**這項不要自行假設，先列出你的疑問回報，等確認語意後再動工**。
13. C1：內鬥可改為雙倍分神的轉換規則完全未實作——先確認觸發時機再排入。
14. C2（歲月靜好難度調整選項）、C3（購買區組成資料校驗）、`rules.md`「2/3共同勝利」語意釐清、`凝聚共識`/`武裝集團` 的死代碼 JSON 宣告清理——優先度最低，排最後。

執行規範：
- 先 `git status --short`，repo 目前可能還有其他未完成工作留下的 dirty file（例如另一份 P1 UI 修正的規劃），不要把不相關的檔案混進你的 commit。
- 每一項都要有自己的 root cause、修正摘要、驗證指令與 proof（比照 `TODO.md` 現有條目風格），完成一項就更新 `TODO.md` 對應區塊；如果該項目原本沒有列在 `TODO.md`，依盤點報告的描述新增一條。
- 建議每完成 1 項（或同梯隊內邏輯相關的幾項一起，例如 `樹立信心` 可以跟既有的 `點燃熱情` 一起修）就單獨 commit 一次，不要累積到最後才一次全部 commit——這個 backlog 項目多、範圍大，分批 commit 可以降低風險，也方便中途檢查進度。
- A1（46個陣營勝利條件）工作量最大，建議拆成兩個子階段：① 先設計資料 schema，把每個陣營的 `win_condition_text` 轉成結構化 `win_conditions`（含確認 `dian_zhuang` 目前是佔位文字，需要先跟我確認正確內容再定案）；② 再擴充 `victory.py` 讀取新資料並補齊 `_count_scope` 涵蓋的 scope 字串，完成後務必寫一個端到端驗證腳本（模擬某個地域型陣營自己達成勝利條件，斷言遊戲正確判定獲勝）。
- 如果一次 session 做不完全部 14 項，做到哪裡就確實記錄到 `TODO.md` 與 `docs/records/rules-audit/RULES_VS_IMPLEMENTATION_GAP_AUDIT_20260710.md`（可在報告末尾補一段「已完成/進行中/待處理」狀態），方便下一個人接手，不需要硬做完全部。

備註：`TODO.md` 仍是權威來源；`docs/records/rules-audit/RULES_VS_IMPLEMENTATION_GAP_AUDIT_20260710.md` 是本次工作的落差清單與優先順序依據，本檔只是新 session 啟動捷徑。
```
