# Next session start prompt（P1 UI 修正 + 規則落差修正，合併派工）

把下面這段貼到新 session 即可。這份合併了兩份既有規劃書，因為目前只會指派給一個人施作，所以由同一個人依序做完兩批工作：

```text
請到 `/Users/benmini/.openclaw/workspace/redline`。這次工作分兩批，請依序做完第一批、commit 過後，再開始第二批，不要交錯進行。

===== 第一批：P1 Playtest UI 三項修正 =====

先讀 `docs/records/playtest-feedback/PLAN_P1_UI_FIXES_20260709.md`，裡面有完整背景、程式碼位置與建議做法，照它處理以下三項（TODO.md 對應段落：「P1：LAN / end-to-end playtest feedback」）：
1. 地圖移動/可達城鎮高亮不要畫大量放射狀直線（不限單一卡牌，所有移動/可移動城鎮顯示流程都要符合）。
2. 選定組織後點可移動城鎮，應先跳出確認視窗，確認才送出移動，取消要保留選擇狀態。
3. Lobby 房間代碼複製功能保留一個即可，移除最上方重複複製入口。

執行前務必先 `git status --short`；規劃書裡第0節記錄了目前 repo 的 staged/unstaged 混合狀態（項目1的程式修正很可能已經做好但沒 commit），照規劃書建議先用 `git restore --staged .` 把狀態攤平再開始，避免跟其他無關的既有 dirty 檔案／未完成工作混在一起。

每項都要補 root cause、修正摘要、驗證指令與 browser proof（proof 放 `docs/records/...`），完成後把 `TODO.md` 對應三個項目更新為 `[done]`。全部三項做完、驗證通過後，只 stage 這三項相關的檔案/hunks，commit 一次（commit message 列點寫清楚三項各自的修正）。

===== 第一批做完並 commit 之後，才開始第二批 =====

===== 第二批：規則資料 vs 程式實作落差修正 =====

先讀 `docs/records/rules-audit/RULES_VS_IMPLEMENTATION_GAP_AUDIT_20260710.md`（第一輪規則落差盤點報告），**再讀 `docs/records/rules-audit/RULES_VS_IMPLEMENTATION_GAP_AUDIT_SECOND_PASS_20260711.md`（第二輪盤點，含 S1-S7 新發現與「第一輪剩餘＋第二輪新發現」的合併優先順序——以第二輪報告的合併順序為準）**。這兩份是這批工作的權威依據，每個項目的完整背景、root cause 與程式碼位置都寫在裡面，不要重新猜測，直接照報告內容查證後動工。

依報告裡「建議的後續改善優先順序」逐項處理，梯隊順序如下（同一梯隊內可自行排序，但不要跳過前面梯隊直接做後面梯隊）：

第一梯隊（影響規則覆蓋率，優先處理）：
1. A1：46個陣營（所有只有 win_condition_text、沒有結構化 win_conditions 的地域/政治類反賊陣營）勝利條件完全未被判定。
2. ~~B1-a：`東洋奧援` 的中介資料檔已損毀/過期~~ ——**已於 2026-07-10 修正並驗證，跳過**，詳見盤點報告該段落與 `TODO.md`。

第二梯隊（明確缺口，修法清楚）：
3. A2：~~非暴力（民運派/性別革命）~~ **已於 2026-07-11 修正並驗證，跳過**；剩下 `紅軍派系`／`民族祭儀` 兩個能力字串未被解析，影響13個陣營。
4. ~~A3：粵／澳門與香港的共用組織規則只有單向生效~~ ——**已於 2026-07-10 修正並驗證，跳過**，詳見盤點報告該段落與 `TODO.md`。
5. ~~`派遣間諜`：犧牲組織與尋找目標順序顛倒~~ ——**2026-07-11 覆核後判定為誤判，非真實 bug，跳過**，詳見盤點報告該段落。
6. ~~`樹立信心`：條件判定看卡牌種類而非購買費用組成~~ ——**已於 2026-07-10 跟 `點燃熱情` 及4個陣營能力一起修正並驗證，跳過**，詳見盤點報告該段落與 `TODO.md`。
7. `組織經驗甲`：TODO.md 現有描述只涵蓋確認流程，實際還有兩段規則子句完全未實作，需要一併補上並更新 TODO 描述範圍。
8. ~~B1-b：`南洋奧援` I級棄牌邏輯等於沒抽沒棄~~ ——**已於 2026-07-11 修正並驗證，跳過**，詳見盤點報告該段落與 `TODO.md`。
9. ~~`情報網`：非反應時機下選項C選了沒效果~~ ——**已於 2026-07-10 修正並驗證，跳過**，詳見盤點報告該段落與 `TODO.md`。

第三梯隊（UI/選擇權相關）：
10. `行動預告`／`行動募資`：本回合買超過1張時沒有選擇要頂哪張牌。
11. ~~`企業人脈`：借用範圍誤排除常設購買區~~ ——**已於 2026-07-10 修正並驗證，跳過**，詳見盤點報告該段落與 `TODO.md`。

第四梯隊（需要先確認規則語意，或優先度較低的保養性質）：
12. B1-c：跨7張區域門檻奧援卡的 AND/OR 語意——**這項不要自行假設，先列出你的疑問回報，等確認語意後再動工**。
13. C1：內鬥可改為雙倍分神的轉換規則完全未實作——先確認觸發時機再排入。
14. C2（歲月靜好難度調整選項）、C3（購買區組成資料校驗）、`rules.md`「2/3共同勝利」語意釐清、`凝聚共識`/`武裝集團` 的死代碼 JSON 宣告清理——優先度最低，排最後。

執行規範：
- 每一項都要有自己的 root cause、修正摘要、驗證指令與 proof（比照 `TODO.md` 現有條目風格），完成一項就更新 `TODO.md` 對應區塊；如果該項目原本沒有列在 `TODO.md`，依盤點報告的描述新增一條。
- 建議每完成 1 項（或同梯隊內邏輯相關的幾項一起，例如 `樹立信心` 可以跟既有的 `點燃熱情` 一起修）就單獨 commit 一次，不要累積到最後才一次全部 commit——這批 backlog 項目多、範圍大，分批 commit 可以降低風險，也方便中途檢查進度。
- A1（46個陣營勝利條件）工作量最大，建議拆成兩個子階段：① 先設計資料 schema，把每個陣營的 `win_condition_text` 轉成結構化 `win_conditions`（含確認 `dian_zhuang` 目前是佔位文字，需要先跟我確認正確內容再定案）；② 再擴充 `victory.py` 讀取新資料並補齊 `_count_scope` 涵蓋的 scope 字串，完成後務必寫一個端到端驗證腳本（模擬某個地域型陣營自己達成勝利條件，斷言遊戲正確判定獲勝）。
- 如果一次 session 做不完全部 14 項，做到哪裡就確實記錄到 `TODO.md` 與 `docs/records/rules-audit/RULES_VS_IMPLEMENTATION_GAP_AUDIT_20260710.md`（可在報告末尾補一段「已完成/進行中/待處理」狀態），方便下一個人接手，不需要硬做完全部。

備註：`TODO.md` 全程都是權威來源；`docs/records/playtest-feedback/PLAN_P1_UI_FIXES_20260709.md` 是第一批的依據，`docs/records/rules-audit/RULES_VS_IMPLEMENTATION_GAP_AUDIT_20260710.md` 是第二批的依據，本檔只是把兩批工作串起來的新 session 啟動捷徑。
```
