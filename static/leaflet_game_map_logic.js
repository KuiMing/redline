const MAP_DATA = {"schema_version": "1.1", "map_id": "world_map_aligned_with_master_town_list", "movement_rules": {"road_cost": 1, "rail_cost": 3}, "towns": {"臺北": {"road": [], "rail": ["基隆", "桃園"], "ruler": ["臺灣"], "camp": ["臺灣", "紅軍", "香港", "藏國"]}, "新北": {"road": ["基隆"], "rail": ["臺北", "桃園"], "ruler": ["臺灣"], "camp": ["臺灣", "紅軍"]}, "基隆": {"road": ["新北", "沖繩", "馬祖"], "rail": ["臺北", "宜蘭"], "ruler": ["臺灣"], "camp": ["臺灣", "紅軍"]}, "桃園": {"road": ["宜蘭"], "rail": ["新北", "新竹"], "ruler": ["臺灣"], "camp": ["臺灣", "紅軍"]}, "新竹": {"road": [], "rail": ["桃園", "苗栗"], "ruler": ["臺灣"], "camp": ["臺灣", "紅軍"]}, "苗栗": {"road": [], "rail": ["新竹", "彰化", "臺中"], "ruler": ["臺灣"], "camp": ["臺灣", "紅軍"]}, "彰化": {"road": [], "rail": ["苗栗", "雲林", "臺中"], "ruler": ["臺灣"], "camp": ["臺灣", "紅軍"]}, "臺中": {"road": ["南投"], "rail": ["彰化", "苗栗"], "ruler": ["臺灣"], "camp": ["臺灣", "紅軍", "香港"]}, "南投": {"road": ["臺中", "花蓮"], "rail": [], "ruler": ["臺灣"], "camp": ["臺灣", "紅軍"]}, "雲林": {"road": [], "rail": ["彰化", "嘉義"], "ruler": ["臺灣"], "camp": ["臺灣", "紅軍"]}, "嘉義": {"road": [], "rail": ["雲林", "臺南"], "ruler": ["臺灣"], "camp": ["臺灣", "紅軍"]}, "臺南": {"road": ["台東"], "rail": ["嘉義", "高雄"], "ruler": ["臺灣"], "camp": ["臺灣", "紅軍"]}, "高雄": {"road": ["澎湖"], "rail": ["臺南", "屏東"], "ruler": ["臺灣"], "camp": ["臺灣", "紅軍", "香港"]}, "屏東": {"road": ["東沙", "佬沃"], "rail": ["高雄", "臺東"], "ruler": ["臺灣"], "camp": ["臺灣", "紅軍"]}, "宜蘭": {"road": ["桃園"], "rail": ["基隆", "花蓮"], "ruler": ["臺灣"], "camp": ["臺灣", "紅軍"]}, "花蓮": {"road": ["南投"], "rail": ["宜蘭", "臺東"], "ruler": ["臺灣"], "camp": ["臺灣", "紅軍"]}, "臺東": {"road": ["臺南"], "rail": ["屏東", "花蓮"], "ruler": ["臺灣"], "camp": ["臺灣", "紅軍"]}, "澎湖": {"road": ["高雄", "馬祖", "金門"], "rail": [], "ruler": ["臺灣"], "camp": ["臺灣", "紅軍"]}, "馬祖": {"road": ["金門", "澎湖", "基隆", "福州"], "rail": [], "ruler": ["臺灣"], "camp": ["臺灣", "紅軍", "反賊"]}, "金門": {"road": ["馬祖", "澎湖", "基隆", "廈門"], "rail": [], "ruler": ["臺灣"], "camp": ["臺灣", "紅軍", "反賊"]}, "東沙": {"road": ["屏東", "觀塘"], "rail": [], "ruler": ["臺灣"], "camp": ["臺灣", "紅軍", "香港"]}, "沖繩": {"road": ["基隆", "福岡"], "rail": [], "ruler": ["東洋"], "camp": ["臺灣", "紅軍", "反賊"]}, "福岡": {"road": ["沖繩", "釜山"], "rail": ["大阪"], "ruler": ["東洋"], "camp": []}, "大阪": {"road": [], "rail": ["福岡", "東京"], "ruler": ["東洋"], "camp": []}, "東京": {"road": [], "rail": ["大阪", "仙臺"], "ruler": ["東洋"], "camp": []}, "仙臺": {"road": [], "rail": ["東京", "札幌"], "ruler": ["東洋"], "camp": []}, "札幌": {"road": ["南薩哈林斯克"], "rail": ["仙臺"], "ruler": ["東洋"], "camp": []}, "釜山": {"road": ["福岡"], "rail": ["首爾"], "ruler": ["東洋"], "camp": []}, "首爾": {"road": ["青島", "大連"], "rail": ["釜山"], "ruler": ["東洋"], "camp": []}, "平壤": {"road": [], "rail": ["清津", "丹東", "吉林"], "ruler": ["東洋"], "camp": ["紅軍", "滿洲"]}, "清津": {"road": [], "rail": ["平壤", "延邊", "海參崴"], "ruler": ["東洋"], "camp": ["紅軍", "滿洲"]}, "延邊": {"road": [], "rail": ["牡丹江", "吉林", "清津", "海參崴"], "ruler": ["紅軍"], "camp": ["紅軍", "滿洲"]}, "丹東": {"road": [], "rail": ["平壤", "大連", "瀋陽"], "ruler": ["紅軍"], "camp": ["紅軍", "滿洲"]}, "牡丹江": {"road": [], "rail": ["延邊", "佳木斯", "哈爾濱", "海參崴"], "ruler": ["紅軍"], "camp": ["紅軍", "滿洲"]}, "佳木斯": {"road": [], "rail": ["牡丹江", "哈爾濱", "黑河", "伯力"], "ruler": ["紅軍"], "camp": ["紅軍", "滿洲"]}, "赤峰": {"road": ["錫林郭勒", "承德", "朝陽"], "rail": ["北京", "通遼"], "ruler": ["紅軍"], "camp": ["紅軍", "蒙古", "滿洲"]}, "承德": {"road": ["赤峰", "天津"], "rail": ["北京", "朝陽"], "ruler": ["紅軍"], "camp": ["紅軍", "滿洲"]}, "朝陽": {"road": ["赤峰"], "rail": ["承德", "阜新"], "ruler": ["紅軍"], "camp": ["紅軍", "蒙古", "滿洲"]}, "阜新": {"road": [], "rail": ["朝陽", "瀋陽"], "ruler": ["紅軍"], "camp": ["紅軍", "蒙古", "滿洲"]}, "北京": {"road": [], "rail": ["天津", "張家口", "石家莊", "錫林郭勒", "赤峰", "承德"], "ruler": ["紅軍"], "camp": ["紅軍", "臺灣", "反賊"]}, "天津": {"road": ["承德"], "rail": ["北京", "瀋陽", "濟南"], "ruler": ["紅軍"], "camp": ["紅軍", "臺灣", "反賊"]}, "張家口": {"road": [], "rail": ["北京", "烏蘭察布"], "ruler": ["紅軍"], "camp": ["紅軍", "蒙古"]}, "烏蘭察布": {"road": [], "rail": ["張家口", "呼和浩特", "錫林郭勒", "賽音山達", "太原"], "ruler": ["紅軍"], "camp": ["紅軍", "蒙古"]}, "錫林郭勒": {"road": ["赤峰", "喬巴山"], "rail": ["烏蘭察布", "興安", "北京"], "ruler": ["紅軍"], "camp": ["紅軍", "蒙古"]}, "呼和浩特": {"road": [], "rail": ["烏蘭察布", "包頭"], "ruler": ["紅軍"], "camp": ["紅軍", "蒙古"]}, "百靈廟": {"road": ["賽音山達", "達蘭札德嘎德"], "rail": ["包頭", "鄂爾多斯"], "ruler": ["紅軍"], "camp": ["紅軍", "蒙古"]}, "包頭": {"road": [], "rail": ["呼和浩特", "巴彥淖爾", "鄂爾多斯", "百靈廟"], "ruler": ["紅軍"], "camp": ["紅軍", "蒙古"]}, "鄂爾多斯": {"road": [], "rail": ["包頭", "延安"], "ruler": ["紅軍"], "camp": ["紅軍", "蒙古"]}, "瀋陽": {"road": [], "rail": ["天津", "長春", "大連", "吉林", "通遼", "阜新", "鞍山"], "ruler": ["紅軍"], "camp": ["紅軍", "滿洲"]}, "通遼": {"road": ["長春"], "rail": ["赤峰", "瀋陽", "白城"], "ruler": ["紅軍"], "camp": ["紅軍", "蒙古", "滿洲"]}, "長春": {"road": ["通遼"], "rail": ["瀋陽", "吉林", "哈爾濱", "白城"], "ruler": ["紅軍"], "camp": ["紅軍", "滿洲"]}, "白城": {"road": [], "rail": ["通遼", "長春", "興安", "齊齊哈爾"], "ruler": ["紅軍"], "camp": ["紅軍", "蒙古", "滿洲"]}, "興安": {"road": [], "rail": ["錫林郭勒", "白城", "呼倫貝爾"], "ruler": ["紅軍"], "camp": ["紅軍", "蒙古", "滿洲"]}, "呼倫貝爾": {"road": ["喬巴山"], "rail": ["興安", "齊齊哈爾", "赤塔"], "ruler": ["紅軍"], "camp": ["紅軍", "蒙古", "滿洲"]}, "齊齊哈爾": {"road": [], "rail": ["大慶", "呼倫貝爾", "黑河", "白城"], "ruler": ["紅軍"], "camp": ["紅軍", "滿洲"]}, "黑河": {"road": [], "rail": ["齊齊哈爾", "海蘭泡", "佳木斯", "哈爾濱"], "ruler": ["紅軍"], "camp": ["紅軍", "滿洲"]}, "吉林": {"road": [], "rail": ["長春", "哈爾濱", "延邊", "平壤", "瀋陽"], "ruler": ["紅軍"], "camp": ["紅軍", "滿洲"]}, "哈爾濱": {"road": [], "rail": ["大慶", "長春", "吉林", "牡丹江", "佳木斯"], "ruler": ["紅軍"], "camp": ["紅軍", "滿洲"]}, "大慶": {"road": [], "rail": ["哈爾濱", "齊齊哈爾"], "ruler": ["紅軍"], "camp": ["紅軍", "滿洲"]}, "大連": {"road": ["青島", "首爾"], "rail": ["鞍山", "丹東"], "ruler": ["紅軍"], "camp": ["紅軍", "滿洲"]}, "鞍山": {"road": [], "rail": ["大連", "瀋陽"], "ruler": ["紅軍"], "camp": ["紅軍", "滿洲"]}, "海參崴": {"road": [], "rail": ["延邊", "牡丹江", "伯力", "清津"], "ruler": ["北國"], "camp": ["滿洲"]}, "伯力": {"road": [], "rail": ["佳木斯", "共青城", "海參崴", "海蘭泡"], "ruler": ["北國"], "camp": ["滿洲"]}, "共青城": {"road": [], "rail": ["伯力", "南薩哈林斯克"], "ruler": ["北國"], "camp": ["滿洲"]}, "南薩哈林斯克": {"road": [], "rail": ["共青城", "札幌"], "ruler": ["北國"], "camp": ["滿洲"]}, "海蘭泡": {"road": [], "rail": ["赤塔", "黑河"], "ruler": ["北國"], "camp": ["滿洲"]}, "赤塔": {"road": [], "rail": ["海蘭泡", "烏蘭烏德", "喬巴山", "呼倫貝爾"], "ruler": ["北國"], "camp": ["滿洲", "臺灣"]}, "太原": {"road": ["延安"], "rail": ["石家莊", "烏蘭察布", "西安"], "ruler": ["紅軍"], "camp": ["紅軍", "臺灣", "反賊"]}, "延安": {"road": ["太原", "銀川"], "rail": ["西安", "鄂爾多斯"], "ruler": ["紅軍"], "camp": ["紅軍", "反賊"]}, "石家莊": {"road": [], "rail": ["鄭州", "北京", "太原", "濟南"], "ruler": ["紅軍"], "camp": ["紅軍", "臺灣", "反賊"]}, "濟南": {"road": ["鄭州"], "rail": ["鄭州", "石家莊", "青島", "天津"], "ruler": ["紅軍"], "camp": ["紅軍", "臺灣", "反賊"]}, "青島": {"road": ["南京", "大連", "首爾"], "rail": ["濟南"], "ruler": ["紅軍"], "camp": ["紅軍", "臺灣", "反賊"]}, "鄭州": {"road": ["濟南"], "rail": ["徐州", "石家莊", "西安", "南陽", "武漢"], "ruler": ["紅軍"], "camp": ["紅軍", "臺灣", "反賊"]}, "徐州": {"road": [], "rail": ["鄭州", "濟南", "合肥", "南京"], "ruler": ["紅軍"], "camp": ["紅軍", "臺灣", "反賊"]}, "南陽": {"road": [], "rail": ["鄭州", "西安", "武漢"], "ruler": ["紅軍"], "camp": ["紅軍", "臺灣", "反賊"]}, "合肥": {"road": [], "rail": ["徐州", "南京", "武漢"], "ruler": ["紅軍"], "camp": ["紅軍", "臺灣", "反賊"]}, "南京": {"road": ["青島"], "rail": ["合肥", "上海", "杭州", "徐州"], "ruler": ["紅軍"], "camp": ["紅軍", "臺灣", "反賊"]}, "上海": {"road": [], "rail": ["南京", "杭州"], "ruler": ["紅軍"], "camp": ["紅軍", "臺灣", "反賊"]}, "杭州": {"road": [], "rail": ["南京", "上海", "南昌", "溫州"], "ruler": ["紅軍"], "camp": ["紅軍", "臺灣", "反賊"]}, "武漢": {"road": [], "rail": ["鄭州", "南陽", "重慶", "合肥", "南昌"], "ruler": ["紅軍"], "camp": ["紅軍", "臺灣", "反賊"]}, "南昌": {"road": [], "rail": ["武漢", "長沙", "杭州", "福州"], "ruler": ["紅軍"], "camp": ["紅軍", "臺灣", "反賊"]}, "長沙": {"road": [], "rail": ["南昌", "貴陽", "廣州", "武漢", "桂林"], "ruler": ["紅軍"], "camp": ["紅軍", "臺灣", "反賊"]}, "桂林": {"road": [], "rail": ["長沙", "南寧", "貴陽"], "ruler": ["紅軍"], "camp": ["紅軍", "臺灣", "反賊"]}, "溫州": {"road": [], "rail": ["福州", "溫州"], "ruler": ["紅軍"], "camp": ["紅軍", "臺灣", "反賊"]}, "福州": {"road": ["馬祖"], "rail": ["廈門", "南昌", "溫州"], "ruler": ["紅軍"], "camp": ["紅軍", "臺灣", "反賊"]}, "廈門": {"road": ["梅州"], "rail": ["福州", "潮州"], "ruler": ["紅軍"], "camp": ["紅軍", "臺灣", "反賊"]}, "潮州": {"road": [], "rail": ["廈門", "梅州", "深圳"], "ruler": ["紅軍"], "camp": ["紅軍", "臺灣", "反賊"]}, "梅州": {"road": ["廈門", "廣州"], "rail": ["潮州"], "ruler": ["紅軍"], "camp": ["紅軍", "臺灣", "反賊"]}, "廣州": {"road": ["梅州"], "rail": ["長沙", "桂林", "南寧", "湛茂", "澳門", "深圳"], "ruler": ["紅軍"], "camp": ["紅軍", "臺灣", "反賊", "香港"]}, "湛茂": {"road": ["南寧"], "rail": ["廣州", "海口"], "ruler": ["紅軍"], "camp": ["紅軍", "臺灣", "反賊"]}, "南寧": {"road": ["湛茂"], "rail": ["桂林", "貴陽", "昆明", "河內"], "ruler": ["紅軍"], "camp": ["紅軍", "臺灣", "反賊"]}, "海口": {"road": [], "rail": ["湛茂", "三亞"], "ruler": ["紅軍"], "camp": ["紅軍", "臺灣", "反賊"]}, "三亞": {"road": [], "rail": ["海口"], "ruler": ["紅軍"], "camp": ["紅軍", "滿洲", "反賊"]}, "深圳": {"road": ["元朗"], "rail": ["潮州", "廣州", "上粉沙打"], "ruler": ["紅軍"], "camp": ["紅軍", "香港", "反賊"]}, "上粉沙打": {"road": [], "rail": ["深圳", "大埔"], "ruler": ["紅軍"], "camp": ["紅軍", "香港"]}, "元朗": {"road": ["深圳", "大埔"], "rail": ["屯門", "荃灣"], "ruler": ["紅軍"], "camp": ["紅軍", "香港"]}, "大埔": {"road": ["元朗"], "rail": ["上粉沙打", "沙田"], "ruler": ["紅軍"], "camp": ["紅軍", "香港"]}, "沙田": {"road": ["葵青", "西貢"], "rail": ["大埔", "九龍城"], "ruler": ["紅軍"], "camp": ["紅軍", "香港"]}, "西貢": {"road": ["沙田"], "rail": ["九龍城"], "ruler": ["紅軍"], "camp": ["紅軍", "香港"]}, "九龍城": {"road": [], "rail": ["沙田", "西貢", "葵青", "觀塘", "油尖旺", "香港城"], "ruler": ["紅軍"], "camp": ["紅軍", "香港"]}, "葵青": {"road": ["沙田"], "rail": ["九龍城", "荃灣", "赤臘角"], "ruler": ["紅軍"], "camp": ["紅軍", "香港"]}, "荃灣": {"road": ["屯門"], "rail": ["葵青", "元朗"], "ruler": ["紅軍"], "camp": ["紅軍", "香港"]}, "屯門": {"road": ["荃灣"], "rail": ["元朗"], "ruler": ["紅軍"], "camp": ["紅軍", "香港"]}, "赤臘角": {"road": ["澳門"], "rail": ["葵青"], "ruler": ["紅軍"], "camp": ["紅軍", "香港"], "type": "機場"}, "香港城": {"road": ["澳門", "赤柱"], "rail": ["九龍城", "柴灣"], "ruler": ["紅軍"], "camp": ["紅軍", "香港"]}, "柴灣": {"road": ["赤柱"], "rail": ["香港城"], "ruler": ["紅軍"], "camp": ["紅軍", "香港"]}, "赤柱": {"road": ["香港城", "柴灣"], "rail": [], "ruler": ["紅軍"], "camp": ["紅軍", "香港"]}, "澳門": {"road": ["赤臘角", "香港城"], "rail": ["廣州"], "ruler": ["紅軍"], "camp": ["紅軍", "香港", "反賊"]}, "油尖旺": {"road": [], "rail": ["九龍城", "深水埗"], "ruler": ["紅軍"], "camp": ["紅軍", "香港"]}, "深水埗": {"road": [], "rail": ["油尖旺", "黃大仙"], "ruler": ["紅軍"], "camp": ["紅軍", "香港"]}, "黃大仙": {"road": [], "rail": ["深水埗", "觀塘"], "ruler": ["紅軍"], "camp": ["紅軍", "香港"]}, "觀塘": {"road": ["東沙"], "rail": ["黃大仙", "九龍城"], "ruler": ["紅軍"], "camp": ["紅軍", "香港"]}, "佬沃": {"road": ["屏東"], "rail": ["馬尼拉"], "ruler": ["南洋"], "camp": ["紅軍", "臺灣", "反賊"], "type": "軍火庫"}, "馬尼拉": {"road": [], "rail": ["佬沃"], "ruler": ["南洋"], "camp": ["紅軍", "臺灣", "反賊"]}, "河內": {"road": [], "rail": ["昆明", "南寧", "胡志明市"], "ruler": ["南洋"], "camp": ["反賊", "香港"]}, "胡志明市": {"road": [], "rail": ["河內"], "ruler": ["南洋"], "camp": ["香港", "反賊"]}, "雅加達": {"road": ["新加坡"], "rail": [], "ruler": ["南洋"], "camp": ["紅軍", "反賊", "臺灣", "維吾爾"]}, "吉隆坡": {"road": [], "rail": ["曼谷", "新加坡"], "ruler": ["南洋"], "camp": ["紅軍", "反賊", "臺灣"]}, "新加坡": {"road": ["雅加達"], "rail": ["吉隆坡"], "ruler": ["南洋"], "camp": ["紅軍", "維吾爾", "反賊", "臺灣"]}, "曼谷": {"road": ["仰光"], "rail": ["吉隆坡", "美斯樂"], "ruler": ["南洋"], "camp": ["紅軍", "反賊", "臺灣"]}, "美斯樂": {"road": ["邦康", "西雙版納", "芒賽", "賀猛"], "rail": ["曼谷"], "ruler": ["南洋"], "camp": ["反賊", "臺灣"], "type": "軍火庫"}, "仰光": {"road": ["曼谷", "賀猛", "邦康", "老街"], "rail": ["密支那"], "ruler": ["南洋"], "camp": ["紅軍", "反賊", "臺灣"]}, "賀猛": {"road": ["仰光", "美斯樂", "邦康"], "rail": [], "ruler": ["南洋"], "camp": ["紅軍", "反賊", "臺灣"], "type": "軍火庫"}, "芒賽": {"road": ["河內", "西雙版納", "美斯樂"], "rail": [], "ruler": ["南洋"], "camp": ["紅軍", "反賊"], "type": "軍火庫"}, "邦康": {"road": ["仰光", "美斯樂", "賀猛", "老街", "西雙版納"], "rail": [], "ruler": ["南洋"], "camp": ["紅軍", "反賊"], "type": "軍火庫"}, "老街": {"road": ["邦康", "仰光", "密支那", "德宏"], "rail": [], "ruler": ["南洋"], "camp": ["紅軍", "反賊"], "type": "軍火庫"}, "密支那": {"road": ["老街", "德宏", "緹祖", "達杭丹"], "rail": ["仰光"], "ruler": ["南洋"], "camp": ["反賊"], "type": "軍火庫"}, "達杭丹": {"road": ["密支那", "迪慶", "緹祖"], "rail": [], "ruler": ["南洋"], "camp": ["藏國"], "type": "軍火庫"}, "緹祖": {"road": ["密支那", "達杭丹", "林芝", "達旺"], "rail": [], "ruler": ["印度"], "camp": ["藏國"]}, "達旺": {"road": ["緹祖", "辛布", "哲古宗"], "rail": [], "ruler": ["印度"], "camp": ["藏國"]}, "辛布": {"road": ["拉旺格拉", "達旺", "日喀則"], "rail": [], "ruler": ["印度"], "camp": ["藏國"]}, "拉旺格拉": {"road": ["辛布", "加德滿都", "日喀則"], "rail": ["邁恩巴德"], "ruler": ["印度"], "camp": ["藏國"]}, "加德滿都": {"road": ["拉旺格拉", "日喀則", "邁恩巴德", "博卡拉"], "rail": [], "ruler": ["印度"], "camp": ["藏國", "紅軍"]}, "博卡拉": {"road": ["加德滿都", "德拉敦"], "rail": [], "ruler": ["印度"], "camp": ["藏國", "紅軍"]}, "德拉敦": {"road": ["博卡拉", "阿里"], "rail": ["德里", "達蘭薩拉"], "ruler": ["印度"], "camp": ["藏國"]}, "達蘭薩拉": {"road": ["列城", "阿里"], "rail": ["德拉敦", "德里", "拉瓦爾品第"], "ruler": ["印度"], "camp": ["藏國"]}, "列城": {"road": ["吉爾吉特", "阿里", "達蘭薩拉"], "rail": [], "ruler": ["印度"], "camp": ["藏國"], "type": "軍火庫"}, "德里": {"road": [], "rail": ["德拉敦", "達蘭薩拉", "加德滿都", "邁恩巴德", "班達拉", "穆恩德戈德"], "ruler": ["印度"], "camp": ["藏國"]}, "邁恩巴德": {"road": ["加德滿都"], "rail": ["德里", "拉旺格拉", "昌達拉里吉", "班達拉"], "ruler": ["印度"], "camp": ["藏國"]}, "昌達拉里吉": {"road": [], "rail": ["邁恩巴德", "班達拉"], "ruler": ["印度"], "camp": ["藏國"]}, "班達拉": {"road": [], "rail": ["德里", "邁恩巴德", "昌達拉里吉", "穆恩德戈德"], "ruler": ["印度"], "camp": ["藏國"]}, "穆恩德戈德": {"road": [], "rail": ["德里", "班達拉", "拜拉庫比"], "ruler": ["印度"], "camp": ["藏國"]}, "拜拉庫比": {"road": [], "rail": ["穆恩德戈德"], "ruler": ["印度"], "camp": ["藏國"]}, "拉瓦爾品第": {"road": ["米蘭沙阿", "霍斯特", "喀布爾", "吉爾吉特"], "rail": ["達蘭薩拉"], "ruler": ["天方"], "camp": ["紅軍", "維吾爾"]}, "吉爾吉特": {"road": ["喀什", "列城", "拉瓦爾品第", "費札巴德"], "rail": [], "ruler": ["天方"], "camp": ["紅軍", "維吾爾"], "type": "軍火庫"}, "費札巴德": {"road": ["喀什", "喀布爾", "吉爾吉特", "杜尚貝"], "rail": [], "ruler": ["天方"], "camp": ["紅軍", "維吾爾", "臺灣"], "type": "軍火庫"}, "喀布爾": {"road": ["費札巴德", "杜尚貝", "拉瓦爾品第", "霍斯特"], "rail": [], "ruler": ["天方"], "camp": ["紅軍", "維吾爾", "臺灣"], "type": "軍火庫"}, "霍斯特": {"road": ["喀布爾", "拉瓦爾品第", "米蘭沙阿"], "rail": [], "ruler": ["天方"], "camp": ["紅軍", "維吾爾"], "type": "軍火庫"}, "米蘭沙阿": {"road": ["霍斯特", "拉瓦爾品第"], "rail": [], "ruler": ["天方"], "camp": ["紅軍", "維吾爾"], "type": "軍火庫"}, "伊斯坦堡": {"road": [], "rail": ["伊德利卜"], "ruler": ["天方"], "camp": ["臺灣", "維吾爾"]}, "伊德利卜": {"road": [], "rail": ["伊斯坦堡", "阿勒坡"], "ruler": ["天方"], "camp": ["維吾爾"], "type": "軍火庫"}, "阿勒坡": {"road": [], "rail": ["伊德利卜", "利雅德"], "ruler": ["天方"], "camp": ["維吾爾"], "type": "軍火庫"}, "利雅德": {"road": [], "rail": ["阿勒坡"], "ruler": ["天方"], "camp": ["維吾爾", "臺灣", "反賊"]}, "英吉沙": {"road": [], "rail": ["喀什", "莎車"], "ruler": ["紅軍"], "camp": ["維吾爾", "紅軍"]}, "莎車": {"road": ["阿里"], "rail": ["和田", "英吉沙"], "ruler": ["紅軍"], "camp": ["維吾爾", "紅軍"]}, "和田": {"road": ["阿克蘇"], "rail": ["莎車", "若羌"], "ruler": ["紅軍"], "camp": ["維吾爾", "紅軍"]}, "阿里": {"road": ["莎車", "列城", "達蘭薩拉", "德拉敦", "日喀則", "那曲"], "rail": [], "ruler": ["紅軍"], "camp": ["藏國", "紅軍"]}, "日喀則": {"road": ["阿里", "加德滿都", "拉旺格拉", "辛布"], "rail": ["拉薩"], "ruler": ["紅軍"], "camp": ["藏國", "紅軍"]}, "拉薩": {"road": [], "rail": ["日喀則", "那曲", "哲古宗"], "ruler": ["紅軍"], "camp": ["藏國", "紅軍"]}, "那曲": {"road": ["阿里", "昌都"], "rail": ["拉薩", "格爾木"], "ruler": ["紅軍"], "camp": ["藏國", "紅軍"]}, "林芝": {"road": ["緹祖", "昌都"], "rail": ["哲古宗"], "ruler": ["紅軍"], "camp": ["藏國", "紅軍"]}, "哲古宗": {"road": ["達旺"], "rail": ["林芝", "拉薩"], "ruler": ["紅軍"], "camp": ["藏國", "紅軍"]}, "格爾木": {"road": ["海南", "玉樹", "果洛", "敦煌", "若羌"], "rail": ["那曲", "西寧"], "ruler": ["紅軍"], "camp": ["藏國", "紅軍", "哈薩克", "蒙古"]}, "玉樹": {"road": ["格爾木", "果洛", "昌都", "阿壩"], "rail": ["西寧"], "ruler": ["紅軍"], "camp": ["藏國", "紅軍"]}, "昌都": {"road": ["玉樹", "林芝", "那曲", "甘孜", "迪慶"], "rail": [], "ruler": ["紅軍"], "camp": ["藏國", "紅軍"]}, "迪慶": {"road": ["昌都", "達杭丹", "甘孜", "木里", "昆明"], "rail": [], "ruler": ["紅軍"], "camp": ["藏國", "紅軍"]}, "木里": {"road": ["迪慶", "甘孜", "昆明", "雅安"], "rail": [], "ruler": ["紅軍"], "camp": ["藏國", "紅軍"]}, "昆明": {"road": ["迪慶", "木里", "西雙版納", "德宏"], "rail": ["成都", "貴陽", "南寧", "河內"], "ruler": ["紅軍"], "camp": ["臺灣", "紅軍", "反賊"]}, "西雙版納": {"road": ["昆明", "德宏", "芒賽", "美斯樂", "邦康"], "rail": [], "ruler": ["紅軍"], "camp": ["紅軍", "反賊"]}, "德宏": {"road": ["昆明", "西雙版納", "老街", "密支那"], "rail": [], "ruler": ["紅軍"], "camp": ["紅軍", "反賊"]}, "貴陽": {"road": [], "rail": ["重慶", "昆明", "南寧", "桂林"], "ruler": ["紅軍"], "camp": ["紅軍", "臺灣", "反賊"]}, "重慶": {"road": [], "rail": ["成都", "貴陽", "西安", "武漢"], "ruler": ["紅軍"], "camp": ["紅軍", "臺灣", "反賊"]}, "成都": {"road": ["阿壩"], "rail": ["西安", "重慶", "昆明", "雅安"], "ruler": ["紅軍"], "camp": ["紅軍", "臺灣", "反賊"]}, "西安": {"road": [], "rail": ["銀川", "蘭州", "延安", "太原", "鄭州", "南陽", "重慶", "成都"], "ruler": ["紅軍"], "camp": ["紅軍", "臺灣", "反賊"]}, "雅安": {"road": ["木里", "甘孜"], "rail": ["成都"], "ruler": ["紅軍"], "camp": ["紅軍", "藏國"]}, "甘孜": {"road": ["雅安", "木里", "迪慶", "阿壩", "昌都"], "rail": [], "ruler": ["紅軍"], "camp": ["紅軍", "藏國"]}, "阿壩": {"road": ["甘孜", "玉樹", "成都", "甘南", "果洛"], "rail": [], "ruler": ["紅軍"], "camp": ["紅軍", "藏國"]}, "果洛": {"road": ["黃南", "玉樹", "甘南", "格爾木", "海南", "阿壩"], "rail": [], "ruler": ["紅軍"], "camp": ["紅軍", "藏國"]}, "海南": {"road": ["果洛", "格爾木", "黃南", "西寧"], "rail": [], "ruler": ["紅軍"], "camp": ["紅軍", "藏國"]}, "黃南": {"road": ["果洛", "甘南", "西寧", "海南"], "rail": [], "ruler": ["紅軍"], "camp": ["紅軍", "藏國"]}, "甘南": {"road": ["阿壩", "果洛", "蘭州", "黃南"], "rail": [], "ruler": ["紅軍"], "camp": ["紅軍", "藏國"]}, "蘭州": {"road": ["甘南", "天祝"], "rail": ["銀川", "西安", "西寧"], "ruler": ["紅軍"], "camp": ["紅軍", "反賊", "臺灣"]}, "西寧": {"road": ["海南", "黃南"], "rail": ["蘭州", "格爾木", "海北"], "ruler": ["紅軍"], "camp": ["紅軍", "反賊", "臺灣", "藏國"]}, "海北": {"road": ["天祝"], "rail": ["西寧", "敦煌"], "ruler": ["紅軍"], "camp": ["紅軍", "藏國"]}, "天祝": {"road": ["蘭州", "海北"], "rail": [], "ruler": ["紅軍"], "camp": ["紅軍", "藏國"]}, "銀川": {"road": ["延安", "額濟納"], "rail": ["烏海"], "ruler": ["紅軍"], "camp": ["紅軍", "反賊", "臺灣"]}, "烏海": {"road": [], "rail": ["銀川", "巴彥淖爾"], "ruler": ["紅軍"], "camp": ["紅軍", "蒙古"]}, "巴彥淖爾": {"road": ["達蘭札德嘎德"], "rail": ["烏海", "包頭", "額濟納"], "ruler": ["紅軍"], "camp": ["紅軍", "蒙古"]}, "額濟納": {"road": ["銀川", "敦煌", "達蘭札德嘎德"], "rail": ["巴彥淖爾", "哈密"], "ruler": ["紅軍"], "camp": ["紅軍", "蒙古"]}, "敦煌": {"road": ["額濟納", "若羌"], "rail": ["格爾木", "海北", "哈密"], "ruler": ["紅軍"], "camp": ["紅軍", "反賊", "蒙古", "哈薩克"]}, "若羌": {"road": ["敦煌"], "rail": ["格爾木", "哈密", "庫爾勒", "和田"], "ruler": ["紅軍"], "camp": ["紅軍", "維吾爾"]}, "哈密": {"road": ["阿爾泰"], "rail": ["敦煌", "若羌", "吐魯番", "奇臺", "額濟納"], "ruler": ["紅軍"], "camp": ["紅軍", "維吾爾", "哈薩克"]}, "奇臺": {"road": [], "rail": ["哈密", "烏魯木齊", "可可托海"], "ruler": ["紅軍"], "camp": ["紅軍", "維吾爾", "哈薩克", "反賊"]}, "吐魯番": {"road": [], "rail": ["哈密", "庫爾勒", "巴音郭楞", "烏魯木齊"], "ruler": ["紅軍"], "camp": ["紅軍", "維吾爾"]}, "可可托海": {"road": ["科布多"], "rail": ["奇臺", "阿勒泰"], "ruler": ["紅軍"], "camp": ["紅軍", "哈薩克"]}, "烏魯木齊": {"road": ["巴音郭楞"], "rail": ["吐魯番", "昌吉", "奇臺"], "ruler": ["紅軍"], "camp": ["紅軍", "哈薩克", "維吾爾"]}, "阿勒泰": {"road": ["烏列蓋", "戈爾諾阿爾泰斯克", "齋桑"], "rail": ["可可托海", "克拉瑪依"], "ruler": ["紅軍"], "camp": ["紅軍", "哈薩克"]}, "昌吉": {"road": [], "rail": ["烏魯木齊", "奎屯"], "ruler": ["紅軍"], "camp": ["紅軍", "維吾爾", "哈薩克", "反賊"]}, "巴音郭楞": {"road": ["烏魯木齊", "伊寧"], "rail": ["庫爾勒", "吐魯番"], "ruler": ["紅軍"], "camp": ["紅軍", "維吾爾", "蒙古"]}, "庫爾勒": {"road": [], "rail": ["若羌", "巴音郭楞", "吐魯番", "庫車"], "ruler": ["紅軍"], "camp": ["紅軍", "維吾爾"]}, "克拉瑪依": {"road": [], "rail": ["阿勒泰", "奎屯", "塔城"], "ruler": ["紅軍"], "camp": ["紅軍", "哈薩克", "維吾爾"]}, "奎屯": {"road": [], "rail": ["克拉瑪依", "博爾塔拉", "昌吉"], "ruler": ["紅軍"], "camp": ["紅軍", "哈薩克", "維吾爾"]}, "庫車": {"road": [], "rail": ["庫爾勒", "阿克蘇"], "ruler": ["紅軍"], "camp": ["紅軍", "維吾爾"]}, "塔城": {"road": ["阿亞古茲"], "rail": ["克拉瑪依"], "ruler": ["紅軍"], "camp": ["紅軍", "哈薩克", "維吾爾"]}, "博爾塔拉": {"road": [], "rail": ["奎屯", "伊寧", "阿克鬥卡"], "ruler": ["紅軍"], "camp": ["紅軍", "哈薩克", "維吾爾", "蒙古"]}, "伊寧": {"road": ["巴音郭楞"], "rail": ["博爾塔拉", "阿克蘇", "札爾肯特"], "ruler": ["紅軍"], "camp": ["紅軍", "哈薩克", "維吾爾"]}, "阿克蘇": {"road": ["和田", "烏什"], "rail": ["庫車", "伊寧", "克孜勒蘇"], "ruler": ["紅軍"], "camp": ["紅軍", "維吾爾"]}, "烏什": {"road": ["和田", "阿克蘇", "春賈", "比什凱克"], "rail": [], "ruler": ["紅軍"], "camp": ["紅軍", "維吾爾"]}, "克孜勒蘇": {"road": ["烏什", "比什凱克"], "rail": ["阿克蘇", "喀什"], "ruler": ["紅軍"], "camp": ["紅軍", "維吾爾"]}, "喀什": {"road": ["塔什干", "費札巴德", "吉爾吉特"], "rail": ["克孜勒蘇", "英吉沙"], "ruler": ["紅軍"], "camp": ["紅軍", "維吾爾"]}, "杜尚貝": {"road": ["塔什干", "喀布爾", "費札巴德"], "rail": [], "ruler": ["北國"], "camp": ["紅軍", "維吾爾"]}, "塔什干": {"road": ["喀什", "杜尚貝"], "rail": ["奇姆肯特"], "ruler": ["北國"], "camp": ["維吾爾"]}, "奇姆肯特": {"road": [], "rail": ["塔什干", "塔拉茲"], "ruler": ["北國"], "camp": ["哈薩克"]}, "塔拉茲": {"road": [], "rail": ["奇姆肯特", "楚城"], "ruler": ["北國"], "camp": ["哈薩克"]}, "楚城": {"road": [], "rail": ["塔拉茲", "阿拉木圖", "比什凱克", "巴爾喀什"], "ruler": ["北國"], "camp": ["哈薩克"]}, "比什凱克": {"road": ["克孜勒蘇", "烏什"], "rail": ["楚城"], "ruler": ["北國"], "camp": ["維吾爾"]}, "巴爾喀什": {"road": [], "rail": ["楚城", "阿克鬥卡", "卡拉干達"], "ruler": ["北國"], "camp": ["哈薩克"]}, "阿克鬥卡": {"road": [], "rail": ["巴爾喀什", "阿亞古茲", "博爾塔拉", "塔爾迪庫爾干"], "ruler": ["北國"], "camp": ["哈薩克"]}, "卡拉干達": {"road": ["阿亞古茲"], "rail": ["鐵米爾套", "巴爾喀什"], "ruler": ["北國"], "camp": ["哈薩克"]}, "阿亞古茲": {"road": ["卡拉干達", "齋桑", "塔城"], "rail": ["塞米伊", "阿克鬥卡"], "ruler": ["北國"], "camp": ["哈薩克"]}, "齋桑": {"road": ["阿亞古茲", "阿勒泰", "鄂斯克門"], "rail": [], "ruler": ["北國"], "camp": ["哈薩克"]}, "鄂斯克門": {"road": ["齋桑"], "rail": ["塞米伊"], "ruler": ["北國"], "camp": ["哈薩克"]}, "塞米伊": {"road": [], "rail": ["阿亞古茲", "鄂斯克門", "巴甫洛達爾", "巴爾瑙爾"], "ruler": ["北國"], "camp": ["哈薩克"]}, "巴甫洛達爾": {"road": ["鄂木斯克"], "rail": ["塞米伊", "巴爾瑙爾", "埃基巴斯圖茲"], "ruler": ["北國"], "camp": ["哈薩克"]}, "鄂木斯克": {"road": ["巴甫洛達爾"], "rail": ["彼得羅巴甫爾", "新西伯利亞"], "ruler": ["北國"], "camp": ["哈薩克"]}, "埃基巴斯圖茲": {"road": [], "rail": ["巴甫洛達爾", "努爾蘇丹"], "ruler": ["北國"], "camp": ["哈薩克"]}, "鐵米爾套": {"road": [], "rail": ["卡拉干達", "努爾蘇丹"], "ruler": ["北國"], "camp": ["哈薩克"]}, "努爾蘇丹": {"road": [], "rail": ["埃基巴斯圖茲", "鐵米爾套", "科克舍套"], "ruler": ["北國"], "camp": ["哈薩克"]}, "科克舍套": {"road": [], "rail": ["努爾蘇丹", "彼得羅巴甫爾", "阿克托別"], "ruler": ["北國"], "camp": ["哈薩克"]}, "彼得羅巴甫爾": {"road": [], "rail": ["科克舍套", "莫斯科", "鄂木斯克"], "ruler": ["北國"], "camp": ["哈薩克"]}, "莫斯科": {"road": [], "rail": ["彼得羅巴甫爾", "阿克托別"], "ruler": ["北國"], "camp": ["哈薩克", "維吾爾", "蒙古", "滿洲"]}, "阿克托別": {"road": [], "rail": ["莫斯科", "科克舍套", "阿特勞"], "ruler": ["北國"], "camp": ["哈薩克"]}, "阿特勞": {"road": [], "rail": ["阿克托別", "埃利斯塔"], "ruler": ["北國"], "camp": ["哈薩克"]}, "埃利斯塔": {"road": [], "rail": ["阿特勞"], "ruler": ["北國"], "camp": ["蒙古"]}, "阿拉木圖": {"road": [], "rail": ["楚城", "春賈", "塔爾迪庫爾干"], "ruler": ["北國"], "camp": ["哈薩克", "維吾爾"]}, "塔爾迪庫爾干": {"road": [], "rail": ["阿拉木圖", "阿克鬥卡"], "ruler": ["北國"], "camp": ["哈薩克"]}, "春賈": {"road": ["烏什"], "rail": ["阿拉木圖", "札爾肯特"], "ruler": ["北國"], "camp": ["維吾爾", "哈薩克"]}, "札爾肯特": {"road": [], "rail": ["伊寧", "春賈"], "ruler": ["北國"], "camp": ["維吾爾", "哈薩克"]}, "新西伯利亞": {"road": [], "rail": ["巴爾瑙爾", "鄂木斯克", "亞巴坎"], "ruler": ["北國"], "camp": ["哈薩克"]}, "巴爾瑙爾": {"road": ["戈爾諾阿爾泰斯克"], "rail": ["塞米伊", "巴甫洛達爾", "新西伯利亞"], "ruler": ["北國"], "camp": ["哈薩克"]}, "戈爾諾阿爾泰斯克": {"road": ["巴爾瑙爾", "阿勒泰", "烏列蓋"], "rail": [], "ruler": ["北國"], "camp": ["哈薩克"]}, "烏列蓋": {"road": ["戈爾諾阿爾泰斯克", "阿勒泰", "科布多"], "rail": [], "ruler": ["北國"], "camp": ["哈薩克", "蒙古"]}, "科布多": {"road": ["烏列蓋", "阿爾泰", "克孜勒"], "rail": [], "ruler": ["北國"], "camp": ["蒙古", "哈薩克"]}, "克孜勒": {"road": ["科布多", "亞巴坎"], "rail": [], "ruler": ["北國"], "camp": ["蒙古"]}, "亞巴坎": {"road": ["克孜勒"], "rail": ["新西伯利亞", "伊爾庫次克"], "ruler": ["北國"], "camp": ["蒙古", "哈薩克"]}, "伊爾庫次克": {"road": ["木倫"], "rail": ["亞巴坎", "烏蘭烏德"], "ruler": ["北國"], "camp": ["蒙古"]}, "木倫": {"road": ["伊爾庫次克", "額爾登特", "科布多", "阿爾泰"], "rail": [], "ruler": ["北國"], "camp": ["蒙古"]}, "阿爾泰": {"road": ["哈密", "科布多", "達蘭札德嘎德", "巴彥洪戈爾", "木倫"], "rail": [], "ruler": ["北國"], "camp": ["蒙古"]}, "巴彥洪戈爾": {"road": ["達蘭札德嘎德", "阿爾泰", "額爾登特", "烏蘭巴托"], "rail": [], "ruler": ["北國"], "camp": ["蒙古"]}, "額爾登特": {"road": ["巴彥洪戈爾", "木倫"], "rail": ["達爾汗", "烏蘭巴托"], "ruler": ["北國"], "camp": ["蒙古"]}, "達爾汗": {"road": [], "rail": ["額爾登特", "烏蘭巴托", "烏蘭烏德"], "ruler": ["北國"], "camp": ["蒙古"]}, "烏蘭烏德": {"road": [], "rail": ["伊爾庫次克", "赤塔", "達爾汗"], "ruler": ["北國"], "camp": ["蒙古"]}, "達蘭札德嘎德": {"road": ["阿爾泰", "巴彥洪戈爾", "額濟納", "烏蘭巴托", "賽音山達", "巴彥淖爾", "百靈廟"], "rail": [], "ruler": ["北國"], "camp": ["蒙古"]}, "烏蘭巴托": {"road": ["達蘭札德嘎德", "巴彥洪戈爾", "喬巴山"], "rail": ["達爾汗", "賽音山達", "額爾登特"], "ruler": ["北國"], "camp": ["蒙古"]}, "賽音山達": {"road": ["達蘭札德嘎德", "百靈廟", "喬巴山"], "rail": ["烏蘭巴托", "烏蘭察布"], "ruler": ["北國"], "camp": ["蒙古"]}, "喬巴山": {"road": ["呼倫貝爾", "錫林郭勒", "賽音山達", "烏蘭巴托"], "rail": ["赤塔"], "ruler": ["北國"], "camp": ["蒙古"]}, "巴黎": {"road": [], "rail": ["日內瓦"], "ruler": ["歐洲"], "camp": []}, "日內瓦": {"road": [], "rail": ["巴黎", "慕尼黑"], "ruler": ["歐洲"], "camp": ["藏國"]}, "慕尼黑": {"road": [], "rail": ["日內瓦"], "ruler": ["歐洲"], "camp": ["維吾爾"]}, "倫敦": {"road": ["華盛頓"], "rail": [], "ruler": ["英美"], "camp": ["香港"]}, "華盛頓": {"road": ["倫敦"], "rail": ["紐約"], "ruler": ["英美"], "camp": []}, "紐約": {"road": [], "rail": ["華盛頓", "多倫多"], "ruler": ["英美"], "camp": []}, "多倫多": {"road": [], "rail": ["紐約", "卡加利"], "ruler": ["英美"], "camp": []}, "卡加利": {"road": [], "rail": ["多倫多", "溫哥華"], "ruler": ["英美"], "camp": []}, "溫哥華": {"road": [], "rail": ["卡加利", "舊金山"], "ruler": ["英美"], "camp": []}, "舊金山": {"road": [], "rail": ["溫哥華", "洛杉磯"], "ruler": ["英美"], "camp": []}, "洛杉磯": {"road": ["奧克蘭"], "rail": ["舊金山"], "ruler": ["英美"], "camp": []}, "奧克蘭": {"road": ["洛杉磯", "雪梨"], "rail": [], "ruler": ["英美"], "camp": []}, "雪梨": {"road": ["奧克蘭"], "rail": [], "ruler": ["英美"], "camp": []}}};
const GEO_COORDS = {"臺北": [121.5654, 25.033], "新北": [121.4657, 25.012], "基隆": [121.7392, 25.1276], "桃園": [121.3, 24.9936], "新竹": [120.9687, 24.8039], "苗栗": [120.8214, 24.5602], "彰化": [120.544, 24.0717], "臺中": [120.6736, 24.1477], "南投": [120.9876, 23.9609], "雲林": [120.4313, 23.7092], "嘉義": [120.4491, 23.4801], "臺南": [120.227, 22.9999], "高雄": [120.3014, 22.6273], "屏東": [120.5488, 22.5519], "宜蘭": [121.753, 24.7591], "花蓮": [121.6015, 23.9872], "臺東": [121.1438, 22.7613], "澎湖": [119.5664, 23.5655], "馬祖": [119.9499, 26.16], "金門": [118.319, 24.432], "東沙": [116.717, 20.7], "沖繩": [127.6792, 26.2124], "福岡": [130.4017, 33.5902], "大阪": [135.5023, 34.6937], "東京": [139.6503, 35.6762], "仙臺": [140.8719, 38.2682], "札幌": [141.3545, 43.0618], "釜山": [129.0756, 35.1796], "首爾": [126.978, 37.5665], "平壤": [125.7625, 39.0392], "清津": [129.7758, 41.7956], "延邊": [129.5089, 42.8913], "丹東": [124.3547, 40.0005], "牡丹江": [129.6332, 44.5517], "佳木斯": [130.3616, 46.8096], "赤峰": [118.9568, 42.2753], "承德": [117.9392, 40.9762], "朝陽": [120.4504, 41.5737], "阜新": [121.6703, 42.0217], "北京": [116.4074, 39.9042], "天津": [117.3616, 39.3434], "張家口": [114.8863, 40.7689], "烏蘭察布": [113.1326, 40.9948], "錫林郭勒": [116.0477, 43.9332], "呼和浩特": [111.7518, 40.8414], "百靈廟": [110.438, 41.698], "包頭": [109.8403, 40.6574], "鄂爾多斯": [109.7813, 39.6083], "瀋陽": [123.4315, 41.8057], "通遼": [122.2434, 43.6529], "長春": [125.3235, 43.8171], "白城": [122.839, 45.619], "興安": [122.067, 46.077], "呼倫貝爾": [119.7658, 49.2116], "齊齊哈爾": [123.9182, 47.3543], "黑河": [127.5286, 50.2458], "吉林": [126.5497, 43.8378], "哈爾濱": [126.6424, 45.7567], "大慶": [125.1038, 46.5893], "大連": [121.6147, 38.914], "鞍山": [122.9946, 41.1086], "海參崴": [131.8869, 43.1155], "伯力": [135.0719, 48.4808], "共青城": [137.0152, 50.5503], "南薩哈林斯克": [142.738, 46.959], "海蘭泡": [127.533, 50.26], "赤塔": [113.4994, 52.034], "太原": [112.5492, 37.8706], "延安": [109.4898, 36.5853], "石家莊": [114.5149, 38.0428], "濟南": [117.1201, 36.6512], "青島": [120.3826, 36.0671], "鄭州": [113.6254, 34.7466], "徐州": [117.2841, 34.2058], "南陽": [112.5285, 32.9907], "合肥": [117.2272, 31.8206], "南京": [118.7969, 32.0603], "上海": [121.4737, 31.2304], "杭州": [120.1551, 30.2741], "武漢": [114.3055, 30.5928], "南昌": [115.8582, 28.682], "長沙": [112.9388, 28.2282], "桂林": [110.2902, 25.2736], "溫州": [120.6994, 27.9949], "福州": [119.2965, 26.0745], "廈門": [118.0894, 24.4798], "潮州": [116.6323, 23.6618], "梅州": [116.1225, 24.2886], "梅州": [116.1225, 24.2886], "廣州": [113.2644, 23.1291], "湛茂": [110.36, 21.27], "南寧": [108.3669, 22.817], "海口": [110.3312, 20.031], "三亞": [109.5119, 18.2528], "深圳": [114.0579, 22.5431], "上粉沙打": [114.135, 22.51], "元朗": [114.022, 22.444], "大埔": [114.171, 22.45], "沙田": [114.195, 22.383], "西貢": [114.27, 22.382], "九龍城": [114.188, 22.329], "葵青": [114.132, 22.354], "荃灣": [114.114, 22.371], "屯門": [113.973, 22.391], "赤臘角": [113.918, 22.309], "香港城": [114.158, 22.282], "柴灣": [114.236, 22.266], "赤柱": [114.214, 22.219], "澳門": [113.5439, 22.1987], "油尖旺": [114.17, 22.307], "深水埗": [114.163, 22.331], "黃大仙": [114.197, 22.342], "觀塘": [114.226, 22.313], "佬沃": [120.5927, 18.196], "馬尼拉": [120.9842, 14.5995], "河內": [105.8542, 21.0278], "胡志明市": [106.6297, 10.8231], "雅加達": [106.8456, -6.2088], "吉隆坡": [101.6869, 3.139], "新加坡": [103.8198, 1.3521], "曼谷": [100.5018, 13.7563], "美斯樂": [99.642, 20.179], "仰光": [96.1561, 16.8661], "賀猛": [98.8, 20.05], "芒賽": [101.3, 21.7], "邦康": [99.2, 22.17], "老街": [103.9707, 22.4856], "密支那": [97.397, 25.384], "達杭丹": [96.5, 28.0], "緹祖": [93.62, 27.1], "達旺": [91.866, 27.586], "辛布": [89.635, 27.472], "拉旺格拉": [88.36, 27.31], "加德滿都": [85.324, 27.7172], "博卡拉": [83.9856, 28.2096], "德拉敦": [78.0322, 30.3165], "達蘭薩拉": [76.3213, 32.219], "列城": [77.5771, 34.1526], "德里": [77.209, 28.6139], "邁恩巴德": [76.337, 32.016], "昌達拉里吉": [79.2, 13.08], "班達拉": [79.65, 21.17], "穆恩德戈德": [75.029, 14.971], "拜拉庫比": [76.365, 12.43], "拉瓦爾品第": [73.0479, 33.5651], "吉爾吉特": [74.464, 35.92], "費札巴德": [70.58, 37.116], "喀布爾": [69.2075, 34.5553], "霍斯特": [69.92, 33.333], "米蘭沙阿": [70.07, 32.984], "伊斯坦堡": [28.9784, 41.0082], "伊德利卜": [36.633, 35.93], "阿勒坡": [37.161, 36.202], "利雅德": [46.6753, 24.7136], "英吉沙": [76.174, 38.929], "莎車": [77.243, 38.414], "和田": [79.927, 37.111], "阿里": [80.105, 32.501], "日喀則": [88.885, 29.267], "拉薩": [91.132, 29.66], "那曲": [92.06, 31.476], "林芝": [94.362, 29.648], "哲古宗": [91.95, 28.45], "格爾木": [94.903, 36.402], "玉樹": [97.008, 33.004], "昌都": [97.178, 31.136], "迪慶": [99.706, 27.826], "木里": [101.28, 27.93], "昆明": [102.8329, 24.8801], "西雙版納": [100.797, 22.009], "德宏": [98.585, 24.433], "貴陽": [106.6302, 26.647], "重慶": [106.5516, 29.563], "成都": [104.0665, 30.5728], "西安": [108.9398, 34.3416], "雅安": [103.013, 29.98], "甘孜": [101.963, 30.05], "阿壩": [102.224, 31.899], "果洛": [100.245, 34.472], "海南": [100.619, 36.286], "黃南": [102.019, 35.517], "甘南": [102.911, 34.986], "蘭州": [103.8343, 36.0611], "西寧": [101.7782, 36.6171], "海北": [100.901, 36.954], "天祝": [103.142, 36.971], "銀川": [106.2309, 38.4872], "烏海": [106.825, 39.673], "巴彥淖爾": [107.387, 40.743], "額濟納": [101.069, 41.958], "敦煌": [94.6619, 40.1421], "若羌": [88.168, 39.023], "哈密": [93.515, 42.819], "奇臺": [89.592, 44.022], "吐魯番": [89.1895, 42.9513], "可可托海": [89.822, 47.205], "烏魯木齊": [87.6168, 43.8256], "阿勒泰": [88.1396, 47.8484], "昌吉": [87.308, 44.011], "巴音郭楞": [86.145, 41.764], "庫爾勒": [86.1746, 41.7259], "克拉瑪依": [84.889, 45.579], "奎屯": [84.902, 44.426], "庫車": [82.963, 41.717], "塔城": [82.9869, 46.7463], "博爾塔拉": [82.066, 44.905], "伊寧": [81.316, 43.922], "阿克蘇": [80.2634, 41.1675], "烏什": [79.224, 41.215], "克孜勒蘇": [76.168, 39.715], "喀什": [75.989, 39.47], "杜尚貝": [68.787, 38.5598], "塔什干": [69.2401, 41.2995], "奇姆肯特": [69.5901, 42.3417], "塔拉茲": [71.3667, 42.9], "楚城": [75.3, 42.817], "比什凱克": [74.5698, 42.8746], "巴爾喀什": [74.9804, 46.8481], "阿克鬥卡": [74.983, 47.833], "卡拉干達": [73.095, 49.804], "阿亞古茲": [80.434, 47.964], "齋桑": [84.867, 47.467], "鄂斯克門": [82.605, 49.948], "塞米伊": [80.249, 50.411], "巴甫洛達爾": [76.95, 52.285], "鄂木斯克": [73.368, 54.989], "埃基巴斯圖茲": [75.322, 51.729], "鐵米爾套": [72.964, 50.054], "努爾蘇丹": [71.433, 51.169], "科克舍套": [69.388, 53.285], "彼得羅巴甫爾": [69.156, 54.873], "莫斯科": [37.6173, 55.7558], "阿克托別": [57.167, 50.284], "阿特勞": [51.883, 47.094], "埃利斯塔": [44.269, 46.308], "阿拉木圖": [76.945, 43.238], "塔爾迪庫爾干": [78.373, 45.015], "春賈": [79.452, 43.538], "札爾肯特": [80.0, 44.167], "新西伯利亞": [82.9357, 55.0084], "巴爾瑙爾": [83.7769, 53.3481], "戈爾諾阿爾泰斯克": [85.96, 51.96], "烏列蓋": [89.967, 48.967], "科布多": [91.642, 48.005], "克孜勒": [94.437, 51.719], "亞巴坎": [91.429, 53.72], "伊爾庫次克": [104.296, 52.286], "木倫": [100.162, 49.635], "阿爾泰": [96.257, 46.372], "巴彥洪戈爾": [100.718, 46.194], "額爾登特": [104.044, 49.027], "達爾汗": [105.922, 49.486], "烏蘭烏德": [107.584, 51.834], "達蘭札德嘎德": [104.425, 43.577], "烏蘭巴托": [106.9057, 47.8864], "賽音山達": [110.136, 44.882], "喬巴山": [114.535, 48.072], "巴黎": [2.3522, 48.8566], "日內瓦": [6.1432, 46.2044], "慕尼黑": [11.582, 48.1351], "倫敦": [-0.1276, 51.5072], "華盛頓": [-77.0369, 38.9072], "紐約": [-74.006, 40.7128], "多倫多": [-79.3832, 43.6532], "卡加利": [-114.0719, 51.0447], "溫哥華": [-123.1207, 49.2827], "舊金山": [-122.4194, 37.7749], "洛杉磯": [-118.2437, 34.0522], "奧克蘭": [174.7633, -36.8485], "雪梨": [151.2093, -33.8688]};

const rulers = [...new Set(Object.values(MAP_DATA.towns).flatMap(t=>t.ruler||[]))].sort();
const camps = [...new Set(Object.values(MAP_DATA.towns).flatMap(t=>t.camp||[]))].sort();
const types = [...new Set(Object.values(MAP_DATA.towns).map(t=>t.type).filter(Boolean))].sort();

const palette = {
  "臺灣":"#3fb6ff","紅軍":"#f04f56","東洋":"#a78bfa","北國":"#67e8f9","南洋":"#22c55e","印度":"#f59e0b","天方":"#fb7185",
  "香港":"#f472b6","藏國":"#eab308","維吾爾":"#34d399","哈薩克":"#60a5fa","蒙古":"#c084fc","滿洲":"#93c5fd","反賊":"#f97316"
};
const typePalette = {"軍火庫":"#ffcf5a","機場":"#93c5fd","default":"#cbd5e1"};

// Player faction ids (e.g. "taiwan_green") don't match the Chinese palette keys above;
// this maps each faction's data-driven "camp" to the palette key covering its whole camp.
const CAMP_COLOR_KEY = {
  red_army: "紅軍", taiwan: "臺灣", hong_kong: "香港", tibet: "藏國",
  uyghur: "維吾爾", kazakh: "哈薩克", mongol: "蒙古", manchuria: "滿洲", rebel: "反賊"
};
// The Taiwan camp shares one palette hue, but the green line / blue line must read as
// green / blue respectively, so those two factions override the camp colour.
const FACTION_COLOR_OVERRIDE = {
  taiwan_green: "#22c55e",  // 綠線 → green
  taiwan_blue: "#3fb6ff",   // 藍線 → blue
};
let factionMeta = new Map();

async function loadFactionMeta() {
  try {
    const res = await fetch('/factions');
    const data = await res.json();
    const meta = new Map();
    const addEntry = (opt) => {
      if (!opt || !opt.id || !opt.camp) return;
      meta.set(opt.id, { camp: opt.camp, label: opt.variant ? `${opt.name}（${opt.variant}）` : (opt.name || opt.id) });
    };
    (data.categories || []).forEach(category => {
      (category.options || []).forEach(opt => {
        addEntry(opt);
        Object.values(opt.variant_details || {}).forEach(addEntry);
      });
    });
    factionMeta = meta;
    renderMap();
    if (lastGameState) applyGameStateToMap(lastGameState);
  } catch (err) {
    console.warn('Failed to load faction metadata for map labels', err);
  }
}

function factionCampColor(factionId) {
  if (FACTION_COLOR_OVERRIDE[factionId]) return FACTION_COLOR_OVERRIDE[factionId];
  const meta = factionMeta.get(factionId);
  if (!meta) return null;
  return palette[CAMP_COLOR_KEY[meta.camp]] || null;
}

function factionLabel(factionId) {
  const meta = factionMeta.get(factionId);
  return meta ? meta.label : factionId;
}

function addOptions(id, arr) {
  const sel = document.getElementById(id);
  arr.forEach(v => {
    const o = document.createElement('option'); o.value = v; o.textContent = v; sel.appendChild(o);
  });
}
addOptions('rulerFilter', rulers); addOptions('campFilter', camps); addOptions('typeFilter', types);

const towns = Object.entries(MAP_DATA.towns).map(([name, raw]) => {
  const c = GEO_COORDS[name];
  return { name, ...raw, lon:c[0], lat:c[1] };
});
const byName = new Map(towns.map(t=>[t.name, t]));

function canonicalEdgeKey(a,b,type) {
  return [type, ...[a,b].sort()].join('::');
}
const linkMap = new Map();
for (const [name,t] of Object.entries(MAP_DATA.towns)) {
  for (const n of (t.road||[])) if (MAP_DATA.towns[n]) linkMap.set(canonicalEdgeKey(name,n,'road'), {source:name,target:n,type:'road'});
  for (const n of (t.rail||[])) if (MAP_DATA.towns[n]) linkMap.set(canonicalEdgeKey(name,n,'rail'), {source:name,target:n,type:'rail'});
}
const links = [...linkMap.values()];

const map = L.map('map', { preferCanvas:true, worldCopyJump:false });
window.__redlinePlayableMap = map;
// Avoid direct use of tile.openstreetmap.org here: local HTML files may be blocked by OSM's tile usage policy
// when Referer is missing. CARTO tiles use OSM data but are more suitable for this standalone viewer.
const cartoLight = L.tileLayer('https://{s}.basemaps.cartocdn.com/light_nolabels/{z}/{x}/{y}{r}.png', { maxZoom:19, attribution:'&copy; OpenStreetMap contributors &copy; CARTO' });
const cartoDark = L.tileLayer('https://{s}.basemaps.cartocdn.com/dark_nolabels/{z}/{x}/{y}{r}.png', { maxZoom:19, attribution:'&copy; OpenStreetMap contributors &copy; CARTO' });
cartoDark.addTo(map);
let currentBasemap = 'cartoDark';

const roadLayer = L.layerGroup().addTo(map);
const railLayer = L.layerGroup().addTo(map);
const markerLayer = L.layerGroup().addTo(map);
const highlightLayer = L.layerGroup().addTo(map);
const buildHighlightLayer = L.layerGroup().addTo(map);
const supportChoiceHighlightLayer = L.layerGroup().addTo(map);
let labelMode = 'auto', showRoad = true, showRail = true;
let currentMarkers = new Map();
let currentSharedBadges = new Map();
let currentVisible = towns.map(t=>t.name);
let lastGameState = null;
let selectedTown = null;
let selectedMoveTargets = [];
let selectedBuildTargets = [];
let pendingMove = null;
let pendingMoveTarget = null;
let lastResolvedMove = null;
let supportChoiceHighlight = null;
let supportChoiceHighlightFocusKey = null;

function clamp(v, min, max) { return Math.max(min, Math.min(max, v)); }
function zoomProgress(z = map.getZoom()) { return clamp((z - 2) / 6, 0, 1); }
function roadWeight(z = map.getZoom()) { return 1.6 + zoomProgress(z) * 5.2; }
function railWeight(z = map.getZoom()) { return 1.8 + zoomProgress(z) * 5.6; }
function markerRadius(z = map.getZoom()) { return 3.2 + zoomProgress(z) * 8.8; }
function markerStroke(z = map.getZoom()) { return 0.9 + zoomProgress(z) * 1.8; }
function railDashArray(z = map.getZoom()) {
  const t = zoomProgress(z);
  const dash = 4 + t * 8;
  const gap = 3 + t * 6;
  return `${dash.toFixed(1)} ${gap.toFixed(1)}`;
}

function colorFor(t) {
  if (t.type) return typePalette[t.type] || typePalette.default;
  return palette[(t.ruler||[])[0]] || '#cbd5e1';
}
function filters() {
  return {
    q: document.getElementById('searchBox').value.trim().toLowerCase(),
    ruler: document.getElementById('rulerFilter').value,
    camp: document.getElementById('campFilter').value,
    type: document.getElementById('typeFilter').value
  };
}
function townMatches(t, f=filters()) {
  return (!f.q || t.name.toLowerCase().includes(f.q)) &&
         (!f.ruler || (t.ruler||[]).includes(f.ruler)) &&
         (!f.camp || (t.camp||[]).includes(f.camp)) &&
         (!f.type || t.type === f.type);
}

function townStateEntries(name) {
  return (lastGameState && lastGameState.map && lastGameState.map.towns && lastGameState.map.towns[name]) || [];
}

function totalOrganizationsInTown(name) {
  return townStateEntries(name).reduce((sum, item) => sum + (item.count || 0), 0);
}

function sharedAccessForTown(name) {
  return ((lastGameState && lastGameState.map && lastGameState.map.shared_access && lastGameState.map.shared_access[name]) || []);
}

function sharedAccessSummary(name) {
  const shared = sharedAccessForTown(name);
  if (!shared.length) return '無';
  return `此城鎮可被 ${shared.join(' / ')} 視為共用組織`;
}

function popupHtml(t) {
  const roads = (t.road||[]).map(n=>`<span class="pill">${n}</span>`).join(' ') || '無';
  const rails = (t.rail||[]).map(n=>`<span class="pill">${n}</span>`).join(' ') || '無';
  const entries = townStateEntries(t.name);
  const total = totalOrganizationsInTown(t.name);
  const controller = entries.length ? entries.slice().sort((a,b)=>(b.count||0)-(a.count||0))[0].player : null;
  const shared = sharedAccessForTown(t.name);
  return `
    <div class="name">${t.name}</div>
    <div>座標：<code>${t.lon.toFixed(3)}, ${t.lat.toFixed(3)}</code></div>
    <div>靜態統治者：${(t.ruler||[]).map(x=>`<span class="pill">${x}</span>`).join(' ') || '無'}</div>
    <div>陣營：${(t.camp||[]).map(x=>`<span class="pill">${x}</span>`).join(' ') || '無'}</div>
    <div>城鎮類型：${t.type ? `<span class="pill">${t.type}</span>` : '一般城鎮'}</div>
    <hr style="border-color:#2b385d;border-style:solid;border-width:1px 0 0;margin:10px 0;">
    <div>當前控制者：${controller ? `<span class="pill">${controller}</span>` : '無組織'}</div>
    <div>當前組織總數：<span class="pill">${total}</span></div>
    <div>共享可用：${shared.length ? shared.map(x=>`<span class="pill">${x}</span>`).join(' ') : '無'}</div>
    <div>共享說明：${shared.length ? `<span class="pill">${sharedAccessSummary(t.name)}</span>` : '無'}</div>
    <hr style="border-color:#2b385d;border-style:solid;border-width:1px 0 0;margin:10px 0;">
    <div>一般道路：${roads}</div>
    <div>鐵路：${rails}</div>`;
}

function resetMoveSelection() {
  selectedMoveTargets = [];
  pendingMove = null;
  pendingMoveTarget = null;
}

function resetBuildSelection() {
  selectedBuildTargets = [];
  buildHighlightLayer.clearLayers();
}

function supportChoiceHighlightKey(payload) {
  if (!payload || payload.mode !== 'support-targets') return null;
  const towns = Array.isArray(payload.towns) ? payload.towns : [];
  return JSON.stringify({
    mode: payload.mode,
    sourceName: payload.sourceName || '',
    prompt: payload.prompt || '',
    focusTown: payload.focusTown || '',
    towns: towns.map(entry => entry?.town || '').filter(Boolean).sort(),
  });
}

function focusSupportChoiceTargets(bounds) {
  if (!bounds.length) return;
  if (bounds.length === 1) {
    map.setView(bounds[0], Math.max(map.getZoom(), 8), { animate: false });
    return;
  }
  map.fitBounds(bounds, { padding: [110, 110], maxZoom: 8 });
}

function selectTownForCurrentMapAction(townName, options = {}) {
  const { autoFocus = true } = options;
  updateInfoPanel(townName);
  resetMoveSelection();
  resetBuildSelection();
  renderMap();
  applyGameStateToMap(lastGameState);
  const didHighlight = renderMovementHighlights(townName, { autoFocus });
  refreshDirectBuildUi();
  window.__lastSelectedTown = townName;
  window.__lastHighlightSuccess = didHighlight;
  return didHighlight;
}

function renderSupportChoiceHighlights(options = {}) {
  const { autoFocus = false } = options;
  supportChoiceHighlightLayer.clearLayers();
  if (!supportChoiceHighlight || supportChoiceHighlight.mode !== 'support-targets') return;
  const towns = Array.isArray(supportChoiceHighlight.towns) ? supportChoiceHighlight.towns : [];
  const bounds = [];
  let focusedBounds = null;
  towns.forEach(entry => {
    const townName = entry?.town;
    const town = byName.get(townName);
    if (!town) return;
    const townBounds = [town.lat, town.lon];
    bounds.push(townBounds);
    if (supportChoiceHighlight.focusTown && supportChoiceHighlight.focusTown === townName) {
      focusedBounds = [townBounds];
    }
    const isFocused = supportChoiceHighlight.focusTown && supportChoiceHighlight.focusTown === townName;
    const outerMarker = L.circleMarker([town.lat, town.lon], {
      radius: Math.max(isFocused ? 18 : 14, markerRadius(map.getZoom()) + (isFocused ? 10 : 6)),
      color: isFocused ? '#facc15' : '#f97316',
      weight: isFocused ? 5 : 4,
      fillColor: isFocused ? '#fde68a' : '#fb923c',
      fillOpacity: isFocused ? 0.34 : 0.22,
      opacity: 1,
    }).addTo(supportChoiceHighlightLayer).bindPopup(`${supportChoiceHighlight.sourceName || '可選目標'}：${entry?.label || townName}`);
    outerMarker.on('click', () => selectTownForCurrentMapAction(townName, { autoFocus: false }));
    const innerMarker = L.circleMarker([town.lat, town.lon], {
      radius: Math.max(isFocused ? 9 : 7, markerRadius(map.getZoom()) + (isFocused ? 2 : 1)),
      color: '#fff7ed',
      weight: 2,
      fillColor: isFocused ? '#facc15' : '#f97316',
      fillOpacity: 0.95,
      opacity: 1,
    }).addTo(supportChoiceHighlightLayer);
    innerMarker.on('click', () => selectTownForCurrentMapAction(townName, { autoFocus: false }));
  });
  if (bounds.length) {
    const hintEl = document.getElementById('interactionHint');
    if (hintEl) {
      const isBuildChoice = ['event_build_organization', 'era_red_build_near_target', 'card_build_organization'].includes(supportChoiceHighlight.choiceKey);
      const focusText = supportChoiceHighlight.focusTown ? ` 已聚焦 ${supportChoiceHighlight.focusTown}。` : '';
      const actionText = isBuildChoice
        ? '請點選橘色城鎮，然後使用左側「在目前城鎮建立組織（效果）」按鈕完成建立。'
        : '請點選橘色城鎮，然後使用左側「瓦解目前城鎮（效果）」按鈕完成瓦解；也可回到選擇視窗確認。';
      // bounds.length is the number of orange markers actually placed on the map, i.e. the
      // exact set of clickable/buildable towns — so the count always matches the highlights.
      const countText = isBuildChoice
        ? `<span class="hint-strong">可建立城鎮：${bounds.length} 個</span>。`
        : `<span class="hint-strong">可選目標：${bounds.length} 個</span>。`;
      hintEl.innerHTML = `${supportChoiceHighlight.sourceName || '當前選擇'}：<span class="hint-strong">${supportChoiceHighlight.prompt || '請依列表選擇目標。'}</span> ${countText}地圖上已用橘色外框標出可選城鎮。${focusText}${actionText}`;
    }
    if (autoFocus) {
      focusSupportChoiceTargets(focusedBounds || bounds);
    }
  }
}

function applySupportChoiceHighlight(payload) {
  const nextKey = supportChoiceHighlightKey(payload);
  const shouldAutoFocus = !!nextKey && nextKey !== supportChoiceHighlightFocusKey;
  supportChoiceHighlight = payload || null;
  supportChoiceHighlightFocusKey = nextKey;
  renderSupportChoiceHighlights({ autoFocus: shouldAutoFocus });
}

function finalizeMoveSelection(fromTown, toTown) {
  lastResolvedMove = { from: fromTown, to: toTown };
  selectedTown = toTown;
  updateInfoPanel(toTown);
  renderMap();
  applyGameStateToMap(lastGameState);
  renderMovementHighlights(toTown);
}

function updateStatusPanel() {
  const currentPlayerEl = document.getElementById('statusCurrentPlayer');
  const selectedTownEl = document.getElementById('statusSelectedTown');
  const hintEl = document.getElementById('interactionHint');

  if (currentPlayerEl) {
    currentPlayerEl.textContent = currentPlayerName() || '未連線';
  }

  if (selectedTownEl) {
    selectedTownEl.textContent = selectedTown || '尚未選取';
  }

  if (hintEl) {
    if (pendingMove) {
      hintEl.innerHTML = pendingMove.error
        ? `移動失敗：<span class="hint-strong">${pendingMove.error}</span>`
        : `正在移動 <span class="hint-strong">${pendingMove.from}</span> → <span class="hint-strong">${pendingMove.to}</span>（${pendingMove.mode}）`; 
    } else if (lastResolvedMove) {
      hintEl.innerHTML = `已完成移動：<span class="hint-strong">${lastResolvedMove.from}</span> → <span class="hint-strong">${lastResolvedMove.to}</span>`;
    } else if (!selectedTown) {
      hintEl.innerHTML = playerHasSafehouse()
        ? '連上遊戲後，點選自己的香港組織城鎮，可同時查看移動與 <span class="hint-strong">安全屋建立範圍</span>。若城鎮有共享組織，會以 <span class="hint-strong">金色外框與 S 標記</span> 顯示。'
        : '連上遊戲後，只有 <span class="hint-strong">當前玩家自己擁有組織</span> 的城鎮可以高亮合法移動；若城鎮具有共享組織，會以 <span class="hint-strong">金色外框與 S 標記</span> 顯示。';
    } else if (eventBuildChoiceForTown(selectedTown)) {
      hintEl.innerHTML = `已選取 <span class="hint-strong">${selectedTown}</span>：事件卡效果允許在此建立組織，請使用左側「在目前城鎮建立組織（事件卡）」按鈕完成。`;
    } else if (playerOwnsTown(selectedTown)) {
      const opts = movementOptionsForTown(selectedTown);
      const buildOpts = playerHasSafehouse() ? buildOptionsForTown(selectedTown) : [];
      const sharedHint = playerHasSharedAccessToTown(selectedTown) ? ' 此城鎮也處於共享組織狀態。' : '';
      hintEl.innerHTML = `已選取 <span class="hint-strong">${selectedTown}</span>：可走一般道路 ${opts.road.length} 條、鐵路 ${opts.rail.length} 條` +
        (buildOpts.length ? `，安全屋可建立 ${buildOpts.length} 個目標。` : '。') + sharedHint;
    } else if (playerHasSharedAccessToTown(selectedTown)) {
      hintEl.innerHTML = `已選取 <span class="hint-strong">${selectedTown}</span>：此城鎮對當前玩家具有 <span class="hint-strong">共享組織</span> 可用性，但互動高亮規則尚未完全 shared-aware。`;
    } else {
      hintEl.innerHTML = `已選取 <span class="hint-strong">${selectedTown}</span>：這不是當前玩家可操作的城鎮。`;
    }
  }
}

function updateInfoPanel(name) {
  const t = byName.get(name);
  if (!t) return;
  const total = totalOrganizationsInTown(name);
  const shared = sharedAccessForTown(name);
  const stateBadge = total > 0 ? `有組織（${total}）` : '無組織';
  const sharedBadge = shared.length ? `共享中（${shared.length}）` : '無共享';
  document.getElementById('info').innerHTML = `${popupHtml(t)}<hr style="border-color:#2b385d;border-style:solid;border-width:1px 0 0;margin:10px 0;"><div>視覺狀態：<span class="pill">${stateBadge}</span> <span class="pill">${sharedBadge}</span></div>`;
}

function shouldShowLabels() {
  return labelMode === 'on' || (labelMode === 'auto' && map.getZoom() >= 5);
}

function clearLayers() {
  roadLayer.clearLayers();
  railLayer.clearLayers();
  markerLayer.clearLayers();
  highlightLayer.clearLayers();
  currentSharedBadges.forEach(marker => {
    try { map.removeLayer(marker); } catch {}
  });
  currentMarkers = new Map();
  currentSharedBadges = new Map();
}

function markerStyleForTown(name, zoom = map.getZoom()) {
  const total = totalOrganizationsInTown(name);
  const ownedByCurrent = playerOwnsTown(name);
  const shared = sharedAccessForTown(name);
  const hasShared = shared.length > 0;
  const base = {
    radius: markerRadius(zoom),
    color: '#07111f',
    weight: markerStroke(zoom),
    fillColor: '#6b7280',
    fillOpacity: 0.32,
    opacity: 0.72,
  };

  if (total <= 0) {
    return hasShared ? {
      ...base,
      color: '#facc15',
      weight: Math.max(base.weight + 1.5, 2.5),
      fillOpacity: 0.46,
      opacity: 0.92,
    } : base;
  }

  const entries = townStateEntries(name);
  const leader = entries.slice().sort((a, b) => (b.count || 0) - (a.count || 0))[0];
  const player = (lastGameState?.players || []).find(p => p.name === leader?.player);
  const controlColor = player?.faction ? (factionCampColor(player.faction) || '#cbd5e1') : '#cbd5e1';

  return {
    radius: Math.max(base.radius + (hasShared ? 3 : 2), hasShared ? 9 : 8),
    color: hasShared ? '#facc15' : (ownedByCurrent ? '#f8fafc' : '#cbd5e1'),
    weight: hasShared ? Math.max(base.weight + 2.5, 3.5) : (ownedByCurrent ? Math.max(base.weight + 1.5, 3) : Math.max(base.weight + 0.5, 2)),
    fillColor: controlColor,
    fillOpacity: ownedByCurrent ? 0.98 : 0.88,
    opacity: 1,
  };
}

function currentPlayerName() {
  return lastGameState && lastGameState.current_player ? lastGameState.current_player : null;
}

function currentPlayerFaction() {
  const player = currentPlayerState();
  return player ? player.faction : null;
}

function canActFromTown(townName) {
  return playerOwnsTown(townName) || playerHasSharedAccessToTown(townName);
}

function playerOwnsTown(townName) {
  if (!lastGameState || !lastGameState.map || !lastGameState.map.towns) return false;
  const entries = lastGameState.map.towns[townName] || [];
  return entries.some(entry => entry.player === currentPlayerName() && (entry.count || 0) > 0);
}

function playerHasSharedAccessToTown(townName) {
  const faction = currentPlayerFaction();
  if (!faction) return false;
  return sharedAccessForTown(townName).includes(faction);
}

function actualTownOwnerName(townName) {
  const entries = townStateEntries(townName);
  if (!entries.length) return null;
  const leader = entries.slice().sort((a, b) => (b.count || 0) - (a.count || 0))[0];
  return leader?.player || null;
}

function labelTextForTown(townName) {
  const total = totalOrganizationsInTown(townName);
  if (total <= 0) return townName;
  const owner = actualTownOwnerName(townName);
  const player = (lastGameState?.players || []).find(p => p.name === owner);
  const factionText = player?.faction ? factionLabel(player.faction) : null;
  return factionText ? `${townName} ${total}（${factionText}）` : `${townName} ${total}`;
}

function sharedDissolveTargetForTown(townName) {
  if (!playerHasSharedAccessToTown(townName)) return null;
  const owner = actualTownOwnerName(townName);
  if (!owner || owner === currentPlayerName()) return null;
  return owner;
}

function movementOptionsForTown(townName) {
  if (!lastGameState || !townName) return { road: [], rail: [] };
  const town = MAP_DATA.towns[townName];
  if (!town) return { road: [], rail: [] };

  if (!canActFromTown(townName)) return { road: [], rail: [] };

  return {
    road: (town.road || []).filter(n => MAP_DATA.towns[n] && !townHasEnemyOrganization(n)),
    rail: railOptionsWithinThree(townName)
  };
}

function townHasEnemyOrganization(townName) {
  const entries = townStateEntries(townName);
  if (!entries.length) return false;
  const currentName = currentPlayerName();
  return entries.some(entry => {
    if (!entry || (entry.count || 0) <= 0) return false;
    if (entry.player === currentName) return false;
    return !playerHasSharedAccessToTown(townName);
  });
}

function railOptionsWithinThree(originTown) {
  const visited = new Set([originTown]);
  const queue = [[originTown, 0]];
  const reachable = new Set();

  while (queue.length) {
    const [townName, distance] = queue.shift();
    if (distance >= 3) continue;
    const town = MAP_DATA.towns[townName] || {};
    for (const nextTown of (town.rail || [])) {
      if (!MAP_DATA.towns[nextTown]) continue;
      if (townHasEnemyOrganization(nextTown)) continue;
      reachable.add(nextTown);
      if (visited.has(nextTown)) continue;
      visited.add(nextTown);
      queue.push([nextTown, distance + 1]);
    }
  }

  reachable.delete(originTown);
  return Array.from(reachable);
}

function currentPlayerState() {
  if (!lastGameState || !mapPlayerId) return null;
  return (lastGameState.players || []).find(p => p.id === mapPlayerId) || null;
}

function playerHasSafehouse() {
  const player = currentPlayerState();
  if (!player || player.faction !== 'hong_kong') return false;
  return ['香港城', '臺北'].some(t => (player.orgs || {})[t] > 0);
}

function buildOptionsForTown(originTown) {
  if (!lastGameState || !originTown || !playerHasSafehouse()) return [];
  if (!canActFromTown(originTown)) return [];

  const maxDistance = 2;
  const visited = new Set([originTown]);
  const queue = [[originTown, 0]];
  const reachable = new Set();

  while (queue.length) {
    const [town, dist] = queue.shift();
    if (dist >= maxDistance) continue;
    const data = MAP_DATA.towns[town] || {};
    const neighbors = new Set([...(data.road || []), ...(data.rail || [])]);
    for (const nxt of neighbors) {
      if (visited.has(nxt)) continue;
      visited.add(nxt);
      reachable.add(nxt);
      queue.push([nxt, dist + 1]);
    }
  }

  return Array.from(reachable).filter(town => MAP_DATA.towns[town]);
}

function renderMovementHighlights(townName, options = {}) {
  const { autoFocus = false } = options;
  highlightLayer.clearLayers();
  // Reset every marker to its base style first so a prior selection's highlight never
  // lingers — this call must produce a complete, order-independent visual.
  currentMarkers.forEach((marker, name) => {
    if (marker.setStyle) marker.setStyle(markerStyleForTown(name, map.getZoom()));
  });
  selectedTown = townName;
  selectedMoveTargets = [];
  lastResolvedMove = null;
  resetBuildSelection();
  updateStatusPanel();
  refreshDirectBuildUi();
  if (!townName) return false;

  const opts = movementOptionsForTown(townName);
  const origin = byName.get(townName);
  if (!origin) return false;

  const canAct = canActFromTown(townName);
  const ownOnly = playerOwnsTown(townName);
  const sharedOnly = !ownOnly && playerHasSharedAccessToTown(townName);

  const originMarker = currentMarkers.get(townName);
  if (originMarker) {
    const originStyle = {
      color: sharedOnly ? '#facc15' : '#ffffff',
      weight: sharedOnly ? 5 : 4,
      fillOpacity: 1,
      radius: Math.max(10, markerRadius(map.getZoom()) + (sharedOnly ? 3 : 2)),
    };
    // A town that has any organisation (own / shared / enemy) keeps its solid faction
    // fill so ownership stays visible; a selected town with no organisation becomes
    // solid white.
    if (totalOrganizationsInTown(townName) <= 0) originStyle.fillColor = '#f8fafc';
    originMarker.setStyle(originStyle);
  }

  if (!canAct) {
    updateStatusPanel();
    refreshDirectBuildUi();
    return false;
  }

  let highlightCount = 0;

  roadLayer.eachLayer(layer => {
    if (layer.setStyle) layer.setStyle({ color:'#d8a04a', opacity:0.12, weight:Math.max(1.5, roadWeight(map.getZoom()) - 1) });
  });
  railLayer.eachLayer(layer => {
    if (layer.setStyle) layer.setStyle({ color:'#ef4444', opacity:0.15, weight:Math.max(2, railWeight(map.getZoom()) - 1), dashArray: railDashArray(map.getZoom()) });
  });

  for (const toName of opts.road) {
    const target = byName.get(toName);
    if (!target) continue;
    selectedMoveTargets.push({ town: toName, mode: 'road' });
    highlightCount += 1;

    const marker = currentMarkers.get(toName);
    if (marker) marker.setStyle({ color: '#ffd166', weight: 5, fillColor: '#f8fafc', fillOpacity: 1, radius: Math.max(10, markerRadius(map.getZoom()) + 2) });
  }

  for (const toName of opts.rail) {
    const target = byName.get(toName);
    if (!target) continue;
    selectedMoveTargets.push({ town: toName, mode: 'rail' });
    highlightCount += 1;

    const marker = currentMarkers.get(toName);
    if (marker) marker.setStyle({ color: '#67e8f9', weight: 5, fillColor: '#f8fafc', fillOpacity: 1, radius: Math.max(10, markerRadius(map.getZoom()) + 2) });
  }

  if (playerHasSafehouse()) {
    const buildTargets = buildOptionsForTown(townName);
    selectedBuildTargets = buildTargets.slice();
    for (const toName of buildTargets) {
      const target = byName.get(toName);
      if (!target) continue;
      L.circleMarker([target.lat, target.lon], {
        radius: Math.max(9, markerRadius(map.getZoom()) + 2),
        color: '#f472b6',
        weight: 3,
        fillColor: '#ec4899',
        fillOpacity: 0.28,
        opacity: 1,
      }).addTo(buildHighlightLayer).bindPopup(`安全屋可建立：${townName} → ${toName}`);
    }
  }

  if (sharedOnly) {
    L.circleMarker([origin.lat, origin.lon], {
      radius: Math.max(12, markerRadius(map.getZoom()) + 5),
      color: '#facc15',
      weight: 3,
      fillOpacity: 0,
      opacity: 1,
      dashArray: '6 4',
    }).addTo(highlightLayer).bindPopup(`共享組織起點：${townName}`);
  }

  if (autoFocus) {
    focusSelectedTown(townName);
  }
  refreshDirectBuildUi();
  updateStatusPanel();
  renderSupportChoiceHighlights();
  return highlightCount > 0 || sharedOnly || selectedBuildTargets.length > 0;
}

function moveOptionForTown(townName) {
  return selectedMoveTargets.find(item => item.town === townName) || null;
}

function sendMoveAction(fromTown, toTown, mode) {
  if (!mapWs || mapWs.readyState !== WebSocket.OPEN) {
    pendingMove = { from: fromTown, to: toTown, mode, error: 'WebSocket 尚未連線' };
    updateStatusPanel();
    return { ok: false, reason: 'socket-not-open' };
  }

  lastResolvedMove = null;
  pendingMove = { from: fromTown, to: toTown, mode };
  updateStatusPanel();
  mapWs.send(JSON.stringify({ action: 'move', from: fromTown, to: toTown, mode }));
  return { ok: true };
}

function eventBuildChoiceForTown(townName) {
  if (!supportChoiceHighlight || !['event_build_organization', 'era_red_build_near_target', 'card_build_organization'].includes(supportChoiceHighlight.choiceKey)) return null;
  const towns = Array.isArray(supportChoiceHighlight.towns) ? supportChoiceHighlight.towns : [];
  const entry = towns.find(item => item?.town === townName);
  if (!entry) return null;
  const resolvedIndex = Number.isFinite(Number(entry.index))
    ? Number(entry.index)
    : towns.findIndex(item => item?.town === townName);
  if (!Number.isFinite(resolvedIndex) || resolvedIndex < 0) return null;
  return { ...entry, index: resolvedIndex };
}

function supportTargetChoiceForTown(townName) {
  if (!supportChoiceHighlight || supportChoiceHighlight.mode !== 'support-targets') return null;
  if (['event_build_organization', 'era_red_build_near_target', 'card_build_organization'].includes(supportChoiceHighlight.choiceKey)) return null;
  const towns = Array.isArray(supportChoiceHighlight.towns) ? supportChoiceHighlight.towns : [];
  const entry = towns.find(item => item?.town === townName);
  if (!entry) return null;
  const resolvedIndex = Number.isFinite(Number(entry.index))
    ? Number(entry.index)
    : towns.findIndex(item => item?.town === townName);
  if (!Number.isFinite(resolvedIndex) || resolvedIndex < 0) return null;
  return { ...entry, index: resolvedIndex };
}

function supportChoiceTownNearLatLng(latlng, maxPixels = 28) {
  if (!supportChoiceHighlight || supportChoiceHighlight.mode !== 'support-targets') return null;
  const towns = Array.isArray(supportChoiceHighlight.towns) ? supportChoiceHighlight.towns : [];
  if (!towns.length || !latlng || !map) return null;
  const clickPoint = map.latLngToContainerPoint(latlng);
  let nearest = null;
  towns.forEach(entry => {
    const townName = entry?.town;
    const town = byName.get(townName);
    if (!town) return;
    const point = map.latLngToContainerPoint([town.lat, town.lon]);
    const distance = clickPoint.distanceTo(point);
    if (distance <= maxPixels && (!nearest || distance < nearest.distance)) {
      nearest = { town: townName, distance };
    }
  });
  return nearest?.town || null;
}

function sendDirectBuildAction(townName) {
  if (!mapWs || mapWs.readyState !== WebSocket.OPEN) {
    return { ok: false, reason: 'socket-not-open' };
  }
  const eventChoice = eventBuildChoiceForTown(townName);
  if (eventChoice) {
    mapWs.send(JSON.stringify({ action: 'resolve_choice', index: eventChoice.index }));
    return { ok: true, eventChoice: true, index: eventChoice.index };
  }
  mapWs.send(JSON.stringify({ action: 'build', town: townName }));
  return { ok: true };
}

function sendDissolveAction(defender, townName) {
  if (!mapWs || mapWs.readyState !== WebSocket.OPEN) {
    return { ok: false, reason: 'socket-not-open' };
  }
  const supportTargetChoice = supportTargetChoiceForTown(townName);
  if (supportTargetChoice) {
    mapWs.send(JSON.stringify({ action: 'resolve_choice', index: supportTargetChoice.index }));
    return { ok: true, supportTargetChoice: true, index: supportTargetChoice.index };
  }
  mapWs.send(JSON.stringify({ action: 'dissolve', defender, town: townName }));
  return { ok: true };
}

function refreshMoveConfirmUi() {
  const confirmBtn = document.getElementById('confirmMoveBtn');
  const cancelBtn = document.getElementById('cancelMoveBtn');
  const hint = document.getElementById('confirmMoveHint');
  if (!confirmBtn || !cancelBtn || !hint) return;

  if (!pendingMoveTarget) {
    confirmBtn.disabled = true;
    cancelBtn.disabled = true;
    hint.innerHTML = '點選可移動城鎮後，這裡會顯示移動確認。';
    return;
  }

  confirmBtn.disabled = false;
  cancelBtn.disabled = false;
  const modeLabel = pendingMoveTarget.mode === 'rail' ? '鐵路' : '道路';
  hint.innerHTML = `確認將組織從 <span class="hint-strong">${pendingMoveTarget.from}</span> 移動到 <span class="hint-strong">${pendingMoveTarget.to}</span>（${modeLabel}）？`;
}

function refreshDirectBuildUi() {
  refreshMoveConfirmUi();
  const btn = document.getElementById('directBuildBtn');
  const hint = document.getElementById('directBuildHint');
  const dissolveBtn = document.getElementById('dissolveBtn');
  const dissolveHint = document.getElementById('dissolveHint');
  if (!btn || !hint || !dissolveBtn || !dissolveHint) return;

  if (!selectedTown) {
    btn.disabled = true;
    btn.textContent = '在目前城鎮建立組織';
    dissolveBtn.disabled = true;
    hint.innerHTML = '選取具有自己組織、共享組織可用性或事件卡允許建立的城鎮後，這裡會顯示是否可直接建立。';
    dissolveHint.innerHTML = '選取具有共享可用性的城鎮後，這裡會顯示是否可對實際組織擁有者發動瓦解。';
    return;
  }

  const eventChoice = eventBuildChoiceForTown(selectedTown);
  if (eventChoice) {
    btn.disabled = false;
    btn.textContent = '在目前城鎮建立組織（效果）';
    hint.innerHTML = `目前選取 <span class="hint-strong">${selectedTown}</span>：目前效果允許在此建立組織；按上方按鈕完成建立。`;
  } else {
    btn.textContent = '在目前城鎮建立組織';
    const sharedOnly = !playerOwnsTown(selectedTown) && playerHasSharedAccessToTown(selectedTown);
    const canAct = canActFromTown(selectedTown);
    if (!canAct) {
      btn.disabled = true;
      hint.innerHTML = `目前選取 <span class="hint-strong">${selectedTown}</span>，但這不是你的組織或共享組織起點。`;
    } else {
      btn.disabled = false;
      if (sharedOnly) {
        hint.innerHTML = `目前選取 <span class="hint-strong">${selectedTown}</span>：你可從 <span class="hint-strong">共享組織</span> 直接發展。`;
      } else {
        hint.innerHTML = `目前選取 <span class="hint-strong">${selectedTown}</span>：你可從自己的組織直接發展。`;
      }
    }
  }

  const supportTargetChoice = supportTargetChoiceForTown(selectedTown);
  if (supportTargetChoice) {
    dissolveBtn.disabled = false;
    dissolveBtn.textContent = '瓦解目前城鎮（效果）';
    dissolveHint.innerHTML = `目前選取 <span class="hint-strong">${selectedTown}</span>：${supportChoiceHighlight.sourceName || '目前效果'} 允許選擇此目標；按上方按鈕完成瓦解。`;
  } else {
    dissolveBtn.textContent = '瓦解目前城鎮組織';
    const dissolveTarget = sharedDissolveTargetForTown(selectedTown);
    if (!dissolveTarget) {
      dissolveBtn.disabled = true;
      dissolveHint.innerHTML = `目前選取 <span class="hint-strong">${selectedTown}</span>：沒有可用的 shared dissolve 目標。`;
    } else {
      dissolveBtn.disabled = false;
      dissolveHint.innerHTML = `目前選取 <span class="hint-strong">${selectedTown}</span>：可瓦解實際擁有者 <span class="hint-strong">${dissolveTarget}</span> 的共享組織。`;
    }
  }
}

function updateDynamicStyles() {
  const z = map.getZoom();
  roadLayer.eachLayer(layer => {
    if (layer.setStyle) layer.setStyle({ color:'#7a6030', weight: roadWeight(z), opacity: 0.24 });
  });
  railLayer.eachLayer(layer => {
    if (layer.setStyle) layer.setStyle({ color: '#42667a', weight: railWeight(z), opacity: 0.28, dashArray: railDashArray(z), lineCap: 'round' });
  });
  markerLayer.eachLayer(layer => {
    const name = [...currentMarkers.entries()].find(([town, marker]) => marker === layer)?.[0];
    if (name && layer.setStyle) {
      layer.setStyle(markerStyleForTown(name, z));
    }
    if (layer.getTooltip && layer.getTooltip()) {
      layer.getTooltip().options.offset = [0, -(markerRadius(z) + 4)];
    }
  });
  currentSharedBadges.forEach((badge, townName) => {
    const town = byName.get(townName);
    if (!town || !badge.setLatLng || !badge.getElement) return;
    badge.setLatLng([town.lat, town.lon]);
  });
  renderSupportChoiceHighlights();
}

function renderMap() {
  clearLayers();
  updateStatusPanel();
  const f = filters();
  const visibleTowns = towns.filter(t => townMatches(t, f));
  currentVisible = visibleTowns.map(t => t.name);
  const visibleSet = new Set(currentVisible);

  for (const link of links) {
    if (!visibleSet.has(link.source) || !visibleSet.has(link.target)) continue;
    const a = byName.get(link.source), b = byName.get(link.target);
    const latlngs = [[a.lat, a.lon], [b.lat, b.lon]];
    const style = link.type === 'road'
      ? { color:'#7a6030', weight:roadWeight(), opacity:0.24 }
      : { color:'#42667a', weight:railWeight(), opacity:0.28, dashArray: railDashArray(), lineCap:'round' };
    const poly = L.polyline(latlngs, style).bindPopup(`${link.type.toUpperCase()}：${link.source} ↔ ${link.target}`);
    if (link.type === 'road' && showRoad) poly.addTo(roadLayer);
    if (link.type === 'rail' && showRail) poly.addTo(railLayer);
  }

  visibleTowns.forEach(t => {
    const marker = L.circleMarker([t.lat, t.lon], markerStyleForTown(t.name)).addTo(markerLayer);
    marker.bindPopup(popupHtml(t), { maxWidth:380 });

    const shared = sharedAccessForTown(t.name);
    if (shared.length) {
      const badge = L.marker([t.lat, t.lon], {
        interactive: false,
        keyboard: false,
        zIndexOffset: 700,
        icon: L.divIcon({
          className: 'shared-badge-wrap',
          html: `<div class="shared-badge ${shared.length > 1 ? 'shared-badge-multi' : ''}" title="${sharedAccessSummary(t.name)}">S${shared.length > 1 ? shared.length : ''}</div>`,
          iconSize: [26, 22],
          iconAnchor: [-2, 14],
        })
      }).addTo(map);
      currentSharedBadges.set(t.name, badge);
    }
    marker.on('click', () => {
      const moveOption = moveOptionForTown(t.name);
      if (selectedTown && moveOption) {
        pendingMoveTarget = { from: selectedTown, to: t.name, mode: moveOption.mode };
        refreshMoveConfirmUi();
        return;
      }

      if (selectedTown && selectedBuildTargets.includes(t.name) && playerHasSafehouse()) {
        if (!mapWs || mapWs.readyState !== WebSocket.OPEN) return;
        mapWs.send(JSON.stringify({ action: 'build', from: selectedTown, town: t.name }));
        return;
      }

      selectTownForCurrentMapAction(t.name, { autoFocus: true });
    });
    currentMarkers.set(t.name, marker);
    if (shouldShowLabels()) marker.bindTooltip(labelTextForTown(t.name), { permanent:true, direction:'top', className:'town-label', offset:[0, -(markerRadius() + 4)] });
  });

  updateDynamicStyles();
  if (selectedTown) {
    renderMovementHighlights(selectedTown, { autoFocus: false });
  }
  renderSupportChoiceHighlights();
}

function fitVisible() {
  const pts = currentVisible.map(n => byName.get(n)).filter(Boolean).map(t => [t.lat, t.lon]);
  if (!pts.length) return;
  map.fitBounds(pts, { padding:[30,30] });
}

function fitAll() {
  const pts = towns.map(t => [t.lat, t.lon]);
  if (!pts.length) return;
  map.fitBounds(pts, { padding:[30,30] });
}

function focusAsia() {
  map.fitBounds([[-5, 68], [55, 145]], { padding:[20,20] });
}

function focusSelectedTown(townName) {
  const origin = byName.get(townName);
  if (!origin || !map) return;

  const moveOptions = movementOptionsForTown(townName);
  const buildOptions = playerHasSafehouse() ? buildOptionsForTown(townName) : [];
  const pts = [[origin.lat, origin.lon]];
  [...moveOptions.road, ...moveOptions.rail, ...buildOptions].forEach(name => {
    const t = byName.get(name);
    if (t) pts.push([t.lat, t.lon]);
  });

  if (pts.length <= 1) {
    map.setView([origin.lat, origin.lon], 9, { animate: false });
    return;
  }

  map.fitBounds(pts, { padding:[80,80], maxZoom: 9 });
}

['searchBox','rulerFilter','campFilter','typeFilter'].forEach(id =>
  document.getElementById(id).addEventListener('input', () => renderMap())
);
document.getElementById('resetFilter').addEventListener('click', () => {
  ['searchBox','rulerFilter','campFilter','typeFilter'].forEach(id => document.getElementById(id).value = '');
  renderMap();
});
document.getElementById('fitFiltered').addEventListener('click', fitVisible);
document.getElementById('fitAll').addEventListener('click', fitAll);
document.getElementById('focusAsia').addEventListener('click', focusAsia);
document.getElementById('confirmMoveBtn').addEventListener('click', () => {
  if (!pendingMoveTarget) return;
  const { from, to, mode } = pendingMoveTarget;
  const result = sendMoveAction(from, to, mode);
  window.__lastMoveRequest = { from, to, mode, ok: result.ok };
  pendingMoveTarget = null;
  refreshMoveConfirmUi();
});
document.getElementById('cancelMoveBtn').addEventListener('click', () => {
  pendingMoveTarget = null;
  refreshMoveConfirmUi();
});
document.getElementById('directBuildBtn').addEventListener('click', () => {
  if (!selectedTown) return;
  sendDirectBuildAction(selectedTown);
});
document.getElementById('dissolveBtn').addEventListener('click', () => {
  if (!selectedTown) return;
  const supportTargetChoice = supportTargetChoiceForTown(selectedTown);
  if (supportTargetChoice) {
    sendDissolveAction(null, selectedTown);
    return;
  }
  const target = sharedDissolveTargetForTown(selectedTown);
  if (!target) return;
  sendDissolveAction(target, selectedTown);
});

map.on('click', (event) => {
  const supportTown = supportChoiceTownNearLatLng(event.latlng);
  if (supportTown) {
    selectTownForCurrentMapAction(supportTown, { autoFocus: false });
  }
});
map.on('zoom', updateDynamicStyles);
map.on('zoomend', () => {
  if (labelMode === 'auto') renderMap(); else updateDynamicStyles();
  if (selectedTown) {
    updateStatusPanel();
  }
});
map.on('popupopen', e => {
  const node = [...currentMarkers.entries()].find(([name, marker]) => marker === e.popup._source);
  if (node) updateInfoPanel(node[0]);
});

window.addEventListener('message', (event) => {
  if (event.origin !== window.location.origin) return;
  const data = event.data || {};
  if (data.type !== 'redline-choice-highlight') return;
  applySupportChoiceHighlight(data.payload || null);
});

function applyGameStateToMap(state) {
  const resolvingMove = pendingMove && !state?.error ? { ...pendingMove } : null;
  lastGameState = state;
  window.__lastMapState = state;
  const pendingChoice = state?.pending_choice || null;
  const mapChoiceKeys = ['event_build_organization', 'era_red_build_near_target', 'card_build_organization', 'support_interaction', 'card_dissolve_interaction', 'intel_network_dissolve_target', 'event_red_dissolve', 'red_army_state_security_target', 'era_red_bonus_dissolve_target'];
  if (!pendingChoice || !mapChoiceKeys.includes(pendingChoice.choice_key)) {
    applySupportChoiceHighlight(null);
  }
  if (resolvingMove) {
    pendingMove = null;
  }
  updateStatusPanel();
  if (!state || state.error) {
    if (state?.error) {
      pendingMove = { ...(pendingMove || {}), error: state.error };
      updateStatusPanel();
    }
    return;
  }
  if (!state.map || !state.map.towns) return;

  highlightLayer.clearLayers();

  markerLayer.eachLayer(layer => {
    const name = [...currentMarkers.entries()].find(([town, marker]) => marker === layer)?.[0];
    if (!name) return;

    if (layer.setStyle) {
      layer.setStyle(markerStyleForTown(name));
    }

    const baseTown = byName.get(name);
    if (baseTown) {
      layer.bindPopup(popupHtml(baseTown), { maxWidth: 380 });
      if (layer.getTooltip()) {
        layer.setTooltipContent(labelTextForTown(name));
      }
    }
  });

  if (resolvingMove) {
    finalizeMoveSelection(resolvingMove.from, resolvingMove.to);
    refreshDirectBuildUi();
    return;
  }

  if (selectedTown) {
    updateInfoPanel(selectedTown);
    renderMovementHighlights(selectedTown, { autoFocus: false });
  }
  refreshDirectBuildUi();
}

window.addEventListener('message', (event) => {
  if (!event.data) return;
  if (event.data.type === 'redline-state') {
    applyGameStateToMap(event.data.state);
    return;
  }
  if (event.data.type === 'redline-choice-highlight') {
    applySupportChoiceHighlight(event.data.payload || null);
  }
});

let mapWs = null;
let mapGameId = null;
let mapPlayerId = null;

window.connectGameMap = function ({ gameId: gid, playerId: pid }) {
  mapGameId = gid;
  mapPlayerId = pid;
  if (!mapGameId || !mapPlayerId) return { ok: false, reason: 'missing-ids' };
  if (mapWs && [WebSocket.OPEN, WebSocket.CONNECTING].includes(mapWs.readyState)) {
    return { ok: true, reused: true };
  }
  if (mapWs) {
    try { mapWs.close(); } catch {}
  }

  mapWs = new WebSocket(`ws://${location.host}/ws/${mapGameId}/${mapPlayerId}`);
  mapWs.onmessage = (event) => {
    const state = JSON.parse(event.data);
    window.lastGameState = state;
    applyGameStateToMap(state);
    try {
      window.parent?.postMessage({ type: 'redline-map-state', state }, window.location.origin);
    } catch (err) {
      console.warn('Failed to sync map state to parent', err);
    }
  };

  return { ok: true };
};

window.__selectTownForTest = function (townName) {
  const marker = currentMarkers.get(townName);
  if (!marker) return { ok: false, reason: 'marker-not-found' };
  updateInfoPanel(townName);
  resetMoveSelection();
  renderMap();
  applyGameStateToMap(lastGameState);
  const didHighlight = renderMovementHighlights(townName, { autoFocus: true });
  refreshDirectBuildUi();
  window.__lastSelectedTown = townName;
  window.__lastHighlightSuccess = didHighlight;
  return {
    ok: true,
    town: townName,
    highlighted: didHighlight,
    road: movementOptionsForTown(townName).road.length,
    rail: movementOptionsForTown(townName).rail.length,
    owns: playerOwnsTown(townName),
    shared: playerHasSharedAccessToTown(townName),
    canAct: canActFromTown(townName)
  };
};

window.__moveFromToForTest = function (fromTown, toTown) {
  resetMoveSelection();
  renderMap();
  applyGameStateToMap(lastGameState);
  const highlighted = renderMovementHighlights(fromTown);
  const option = moveOptionForTown(toTown);
  if (!highlighted || !option) {
    return { ok: false, reason: 'target-not-reachable', fromTown, toTown, highlighted };
  }
  const result = sendMoveAction(fromTown, toTown, option.mode);
  return { ok: !!result.ok, fromTown, toTown, mode: option.mode };
};

window.__clickMoveTargetForTest = function (townName) {
  const marker = currentMarkers.get(townName);
  if (!marker) return { ok: false, reason: 'marker-not-found', townName };
  marker.fire('click');
  return { ok: true, townName, pendingMoveTarget: pendingMoveTarget ? { ...pendingMoveTarget } : null };
};
window.__confirmPendingMoveForTest = function () {
  const btn = document.getElementById('confirmMoveBtn');
  if (!btn || btn.disabled) return { ok: false, reason: 'confirm-disabled' };
  btn.click();
  return { ok: true };
};
window.__cancelPendingMoveForTest = function () {
  const btn = document.getElementById('cancelMoveBtn');
  if (!btn || btn.disabled) return { ok: false, reason: 'cancel-disabled' };
  btn.click();
  return { ok: true };
};
window.__mapDebugStateForTest = function () {
  return {
    selectedTown,
    pendingMoveTarget: pendingMoveTarget ? { ...pendingMoveTarget } : null,
    reachableFromSelected: selectedMoveTargets.map(t => t.town),
  };
};

window.selectTownForCurrentMapAction = selectTownForCurrentMapAction;
window.__dissolveFromSharedForTest = function (townName) {
  resetMoveSelection();
  renderMap();
  applyGameStateToMap(lastGameState);
  updateInfoPanel(townName);
  renderMovementHighlights(townName, { autoFocus: true });
  refreshDirectBuildUi();
  const defender = sharedDissolveTargetForTown(townName);
  if (!defender) {
    return { ok: false, reason: 'no-shared-dissolve-target', townName };
  }
  const result = sendDissolveAction(defender, townName);
  return { ok: !!result.ok, townName, defender };
};

window.__directBuildForTest = function (townName) {
  resetMoveSelection();
  renderMap();
  applyGameStateToMap(lastGameState);
  updateInfoPanel(townName);
  selectedTown = townName;
  refreshDirectBuildUi();
  const button = document.getElementById('directBuildBtn');
  if (!button || button.disabled) {
    return { ok: false, reason: 'direct-build-disabled', townName, eventChoice: !!eventBuildChoiceForTown(townName) };
  }
  const result = sendDirectBuildAction(townName);
  return { ok: !!result.ok, townName, eventChoice: !!result.eventChoice, index: result.index };
};

window.__advanceToActionForTest = function () {
  if (!mapWs || mapWs.readyState !== WebSocket.OPEN) return { ok: false, reason: 'socket-not-open' };
  mapWs.send(JSON.stringify({ action: 'advance' }));
  return { ok: true };
};

renderMap();
setTimeout(focusAsia, 100);
loadFactionMeta();
