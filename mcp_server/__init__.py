"""REDLINE MCP server: lets an MCP-compatible LLM client play REDLINE
end-to-end by driving the real HTTP + WebSocket API (server/main.py,
server/lobby_routes.py) that the browser client already uses.

See docs/mcp_server.md for setup, environment variables, and a full
create-room-to-first-turn example.
"""
